# 智慧农业 - 数据库设计说明文档

> 版本：v1.0
> 配套：
> - `01_智慧农业_基本功能清单.md` —— 需求来源
> - `02_数据类型与ER设计.md` —— 概念设计（实体 / 属性 / 关系 / ER 图）
> - `03_数据库建表SQL.sql` —— 可直接执行的建表脚本
> - `数据库设计.md` —— 物理层初稿（v0.1，本说明的早期版本，字段与本三份一致）

---

## 1. 文档目的

本文档说明智慧农业平台后端数据库的**设计思路、技术选型、关键决策与数据流**，帮助组内同学理解"为什么这么设计"，便于后续开发、联调与答辩。不重复罗列建表语句（见 `03_数据库建表SQL.sql`）和 ER 图（见 `02_数据类型与ER设计.md`）。

---

## 2. 设计目标与非功能性需求

从功能清单可归纳出几个硬约束，直接决定了表结构：

| 约束 | 来源 | 设计响应 |
|---|---|---|
| **高频写入** | 传感器持续上报，数据量大 | `sensor_data` 独立成时间序列表，毫秒时间戳，复合索引，主键自增 |
| **实时性** | 实时温湿度、设备在线状态 | 在线状态由心跳时间推导；最新值用"取最新一条"查询 |
| **扩展性** | 未来加光照/土壤pH等指标 | 传感器数据用 `metric` 字段区分指标，加指标不改表 |
| **团队分工** | 前端接口已定，多人并行 | 不加物理外键，避免锁表与耦合；用索引 + 应用层保证 |
| **可追溯** | 告警、灌溉操作要留痕 | `alarm`、`control_log` 两张日志表 |

---

## 3. 技术选型

| 项 | 选择 | 理由 |
|---|---|---|
| 数据库 | MySQL 8.x（`mysql-connector-j-8.0.33.jar`） | 课程/团队既定技术栈，`server/DBUtil.java` 已接通 |
| 存储引擎 | InnoDB | 支持事务、行级锁、崩溃恢复 |
| 字符集 | `utf8mb4` | 中文 + emoji 完整支持 |
| 外键 | **不使用物理外键** | 高频写入 + 多人分工下外键有开销、易锁表；用索引 + 应用层保证一致性 |
| 主键 | 业务字符串（plot/device）+ 自增 BIGINT（日志类） | 见第 6 节决策 |

---

## 4. 数据模型总览

7 张核心表，2 类：**业务主数据**（用户、地块、设备、阈值）与**流水/日志**（传感器数据、告警、控制日志）。

| 表 | 中文名 | 类别 | 关键作用 |
|---|---|---|---|
| `user` | 用户表 | 主数据 | 登录、角色权限 |
| `plot` | 地块表 | 主数据 | 组织设备与告警的最小单元 |
| `device` | 设备表 | 主数据 | 传感器/灌溉设备，在线与运行状态 |
| `sensor_data` | 传感器数据表 | 流水（高频） | 温湿度时间序列 |
| `plot_threshold` | 阈值配置表 | 主数据 | 告警判定参数（每地块一行） |
| `alarm` | 告警表 | 日志 | 告警记录与处理状态 |
| `control_log` | 控制日志表 | 日志 | 灌溉操作留痕 |

关系（详情见 `02_数据类型与ER设计.md` 第三节）：

```
user 1 ── N plot         （管理员管理地块，可选）
plot 1 ── N device       （一个地块多台设备）
device 1 ─ N sensor_data （一台设备持续上报）
device 1 ─ N control_log （灌溉操作留痕）
plot 1 ── N alarm        （地块产生告警；离线告警额外关联 device）
plot 1 ── 1 plot_threshold  （每地块一行阈值）
```

---

## 5. 各表设计说明

### 5.1 user —— 用户表

- 三种角色 `farmer / admin / sysadmin` 存英文枚举值，前端显示的"农户/农场管理员/系统管理员"由后端映射。
- `password` 只存哈希（BCrypt），**不存明文**。
- `token` 由后端登录时签发（JWT/会话），**不落库**。

### 5.2 plot —— 地块表

- 主键用业务字符串 `P001`，**不用自增 int**：前端 URL、设备绑定都用它，必须稳定可读。
- `area` 只存数字（如 `2.50`），单位"亩"由前端拼，避免存 `'2.5亩'` 这种带单位字符串。
- 列表里看到的"设备数 / 在线数 / 实时温湿度"是**聚合派生值**，查询时算，不建字段。

### 5.3 device —— 设备表

- `type` 三类：土壤湿度传感器 / 温度传感器 / 灌溉设备。
- "是否可控制 `controllable`"不存，由 `type == '灌溉设备'` 推导。
- `online` 由 `last_heartbeat` 判定：心跳超时 → 置 0 并触发离线告警；这是**派生态**，不单独存是否在线之外的冗余。
- `running` 只对灌溉设备有意义，其他设备恒 0。

### 5.4 sensor_data —— 传感器数据表（核心、高频）

- 一张表装两种指标，靠 `metric`（`temp` / `humidity`）区分；以后加光照、土壤 pH 只加枚举值，不改表。
- `collected_at` 用 `DATETIME(3)`（毫秒），保证同一设备高频上报不乱序、可去重。
- 复合索引 `(device_id, metric, collected_at)` 直接覆盖三条典型查询：按设备查历史、按指标查、按时间范围查。
- 实时值 = 每个 `(plot_id, metric)` 取 `collected_at` 最新一条；地块级历史 join `device` 拿 `plot_id` 再聚合。
- ⚠️ 数据量大后要按时间归档/清理，本设计预留了纯追加、无更新的结构便于分表。

### 5.5 plot_threshold —— 阈值配置表（每地块一行）

- 每个地块一条阈值记录，主键即 `plot_id`（与 `plot` 1:1）。
- 未配置阈值的地块，后端判定时使用默认值（湿度 40%、温度 35℃）。
- 阈值告警触发时按**所属地块**取阈值：`sensor_data` → join `device` 拿 `plot_id` → 查 `plot_threshold` → 比对 → 插 `alarm`。

### 5.6 alarm —— 告警表

- `alarm_type / level / status` 直接存前端要显示的中文值（与 mock 一致），后端判定阈值时写库、前端原样展示，省一层映射。
- `value` 是展示型字段（带单位），存字符串。
- `device_id` 可空：阈值告警（湿度/温度）不填，**离线告警**才填触发设备。
- 阈值告警触发链：`sensor_data` 新值低于/高于该地块的 `plot_threshold` → 插一条 `alarm`。

### 5.7 control_log —— 控制日志表

- 每次 `controlIrrigation` 插一条，实现控制留痕。
- `deviceName / plotName` 展示时 join 出来，不冗余存储。

---

## 6. 关键设计决策与理由

1. **主键两条风格**
   - 业务主数据（`plot.id`、`device.id`）用稳定业务字符串（`P001`/`D001`），因为前端 URL、绑定关系依赖它。
   - 流水/日志（`sensor_data`、`alarm`、`control_log`）用自增 `BIGINT`，写入快、无业务含义。

2. **不加物理外键**
   - 高频写入（`sensor_data`）+ 多人分工时，物理外键有额外开销、易锁表、联调互相阻塞。
   - 改用：索引 + 应用层保证一致性 + 文档约定。

3. **聚合/派生字段不落库**
   - 设备数、在线数、实时温湿度、`controllable`、`plotName` 等都在查询时算，避免冗余和不一致。

4. **中文枚举值直接存库**
   - `alarm.alarm_type/level/status`、`control_log.action/result` 存中文，与前端 mock 一致，减少前后端映射层。

5. **时间戳统一 `DATETIME`**，高频的 `sensor_data.collected_at` 用 `DATETIME(3)`。

---

## 7. 数据流说明

```
硬件传感器 → 采集 → MQTT 上报 → 后端接收
                                  ├─ 写入 sensor_data（温湿度）
                                  ├─ 更新 device.last_heartbeat / online
                                  ├─ 按地块比对 plot_threshold → 触发 alarm
                                  └─ 心跳超时 → 置离线 + 离线 alarm

前端操作：
  灌溉开关 → 后端 → MQTT 下发硬件 → 结果回传 → 写 control_log + 更新 device.running
  查看历史 → 后端查 sensor_data → 前端折线图
  设备绑定 → 后端维护 device（新增/解绑）
  智能问答 → RAG 知识库（不在 MySQL）
```

---

## 8. 命名与约定

1. 表名/字段全小写下划线（`snake_case`），代码里保持一致。
2. 主键统一 `id`；外键字段命名 `xxx_id`（如 `plot_id`、`device_id`）。
3. 索引命名 `idx_表名_字段`（如 `idx_plot`、`idx_dev_metric_time`）。
4. 时间字段：创建 `created_at`、更新 `updated_at`、处理 `handled_at`、采集 `collected_at`、心跳 `last_heartbeat`。
5. 布尔字段用 `TINYINT`（0/1），如 `online`、`running`。

---

## 9. 初始化步骤

1. 确认库名：`server/DBUtil.java` 中 `DB` 常量当前是 `suiyuanhao`，`03_数据库建表SQL.sql` 中写的是 `farm` —— **先统一成一个**（改脚本或改 `DBUtil`）。
2. 执行 `03_数据库建表SQL.sql`（MySQL 客户端或 Navicat/DBeaver 导入）。
3. 确认种子数据：3 个用户、2 个地块、5 台设备、1 行阈值已插入。
4. 用 `server/DBTest.java` 验证连通并列出 7 张表。
5. 把 3 个占位密码替换为 BCrypt 生成的哈希（`user.password` 字段）。

---

## 10. 待确认 / 扩展点

- [ ] 库名统一（`farm` vs `suiyuanhao`）。
- [ ] 密码哈希方案（BCrypt 库选型）。
- [x] 每地块独立阈值（已采用 `plot_threshold`，每地块一行）。
- [ ] `sensor_data` 数据量增长后的归档/清理策略。
- [ ] 用户与地块的权限绑定粒度（当前仅按角色区分，未做"某管理员只能看某几块地"）。
