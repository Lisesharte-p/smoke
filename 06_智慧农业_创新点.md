cla# 智慧农业 - 创新点设计方案

> 定位：本项目的**创新点梳理与取舍建议**，用于组内讨论、开发规划与答辩准备。
> 现状：系统已具备「感知（温湿度传感器）→ 传输（MQTT）→ 存储（7 张表）→ 展示（实时/历史）→ 控制（灌溉）→ 告警（静态阈值）→ 智能问答（RAG 占位）」完整闭环。
> 创新主线一句话：**把系统从「被动响应阈值」变成「主动做决策」**。

---

## 一、创新点主线

现有系统的告警是"静态阈值 + 人工响应"（湿度低于阈值 → 告警 → 人手动开灌溉开关），这是所有同类课程项目的常见形态。

创新方向围绕一条主线展开：

> **从告警响应（被动）到智能决策（主动）**
>
> 静态阈值（人人都有）→ 动态阈值 + 气象联动（①⑤）→ 异常自诊断（③）→ 数据驱动的智能助手（④）

整条线逻辑自洽，答辩时能讲成"一个故事"而不是"三个功能"。

---

## 二、创新点清单

### ① 灌溉决策引擎：从"手动按钮"到"自动决策"（最推荐，答辩主亮点）

**现状**：湿度低于阈值 → 告警 → 人打开关。与所有课程项目雷同。

**创新**：引入 `irrigation_plan`（灌溉计划）概念——系统不仅告警，还会**自动给出并执行"灌溉多少分钟、什么时候浇"**，决策依据三合一：

1. 当前土壤湿度 + **湿度下降速率**（斜率陡 = 需水急，优先灌溉）
2. **未来 24h 降雨概率**（接免费天气 API：和风天气 / 高德天气，真机或假数据均可演示）
3. **作物 + 生长阶段**（如番茄：苗期需水少、结果期需水多）

**落地方式**（基本不动现有表结构）：
- `plot` 增加列 `growth_stage`（生长阶段），或新建 `crop_profile` 表（作物 × 阶段 × 适宜湿度区间）
- `control_log` 增加 `duration_minutes`（灌溉时长）与 `water_used_l`（用水量）
- 判定逻辑：`sensor_data` → 算下降速率 → join 地块 + 作物画像 → 查降雨预报 → 生成灌溉建议

**演示效果**：同一地块，预报有雨时系统显示"已自动推迟灌溉"，无雨时显示"建议灌溉 20 分钟"并可一键执行。

**答辩话术**：「从阈值告警升级为预测性灌溉，减少无效浇水。」

---

### ② 气象联动 + 节水量化（与①配套，成果可量化）

**创新**：接天气 API 后顺带做一张**节水看板**：

- 统计每个地块的月用水量
- 对比"人工习惯性灌溉"的估算用水，展示"系统帮你省了 XX% 水"

**实现成本**：低。`control_log` 聚合 + 一个 ECharts 柱状图即可。

**价值**：答辩时"数据说话"比功能列表更有说服力。

---

### ③ 异常检测：超越阈值的"设备自诊断"（成本极低，亮点足）

**现状**：告警只判断"是否低于阈值"。

**创新**：对 `sensor_data` 用滑动窗口计算**一阶差分/斜率 + 方差**，识别两类阈值发现不了的异常：

- **数值骤变**：湿度 1 分钟内从 60% 掉到 5% → 大概率是传感器故障或水管爆裂，而非缺水 → 生成"传感器异常"告警，提醒检查硬件而不是浇水
- **长时不变**：连续 N 小时读数恒为同一值 → 传感器"假在线"（卡死）

**落地方式**：纯算法层，不碰表结构，只新增一种 `alarm_type`（如 `传感器异常`）。

**演示**：现场改数据演示——正常波动不告警、骤变立刻告警。

---

### ④ 智能体从"关键词问答"升级为"带数据的农技助手"（工具调用 Agent）

**现状**：`getChatReply` 是关键词匹配，答案与地块实测数据完全无关。

**创新**：让问答**带上实时数据上下文**，甚至能"查 → 答 → 一键执行"：

- 用户问"P001 该浇水吗？" → 后端先查该地块最新湿度 → 回答："当前湿度 38%，低于该地块阈值 40%，建议灌溉 20 分钟"，并附带**一键执行按钮**（复用现有 `controlIrrigation` 接口，加二次确认）
- 本质是 **Agent + Tool Use**：把 `getRealtime`、`getThresholds`、`controlIrrigation` 暴露为助手的工具
- 课程技术栈即可实现（规则 / 小型 RAG），答辩讲"工具调用"概念很加分

---

### ⑤ 动态阈值：阈值随作物生长阶段自动变化

**现状**：`plot_threshold` 是静态配置（每地块一行固定 40% / 35℃）。

**创新**：阈值不再是死的，而是 `crop_profile`（作物品种 × 生长阶段）里的推荐值，随 `growth_stage` 自动切换；用户手动改的阈值只是"临时覆盖"。

**落地方式**：`plot_threshold` 表加字段即可。

**价值**：既是①的底座，单独拿出来也是一个清晰的设计亮点。

---

### ⑥ 多指标环境画像（扩展项，呼应已有好设计）

**现状**：`sensor_data` 用 `metric` 字段区分指标，本就是扩展性设计。

**创新**：顺势把它做成真的——增加**光照度、CO2 浓度、土壤 EC/pH** 指标，做"环境适宜度雷达图"：每项与作物适宜区间比对，算出一个 0–100 的**适宜度综合分**。

**实现成本**：中。若不想扩硬件，用 mock 数据 + 现有 `metric` 字段即可演示，展示的是**架构的扩展性**。

---

## 三、取舍建议

若只选 **2–3 个**落地，建议 **① + ③ + ④**。

| 创新点 | 实现成本 | 演示冲击力 | 答辩深度 |
|---|---|---|---|
| ① 决策引擎 + 气象 | 中（天气 API + 一段逻辑） | ★★★★★ | ★★★★★ |
| ② 节水量化看板 | 低（聚合 + 图表） | ★★★ | ★★★ |
| ③ 异常检测 | 低（纯算法） | ★★★★ | ★★★★ |
| ④ 数据智能体 | 中 | ★★★★ | ★★★★★ |
| ⑤ 动态阈值 | 低 | ★★★ | ★★★ |
| ⑥ 多指标画像 | 中 | ★★★★ | ★★★ |

**主线串联**：从告警响应到智能决策——静态阈值（人人都有）→ 动态阈值 + 气象联动（①⑤）→ 异常自诊断（③）→ 数据驱动的智能助手（④）。

---

## 四、落地优先级建议（开发顺序）

1. **⑤ 动态阈值 + ① 决策引擎**：先加 `crop_profile` / `growth_stage`，再写灌溉决策逻辑，形成核心卖点。
2. **③ 异常检测**：纯算法，顺手加一种告警类型，风险最低。
3. **④ 数据智能体**：在助手接口里接实时数据 + 一键执行，提升交互深度。
4. **② 节水看板 / ⑥ 多指标画像**：答辩冲刺阶段补充，增强数据可视化表现力。