# 智慧农业 - 数据库设计

> 依据：`01_智慧农业_基本功能清单.md` + `frontend/`（`api.js` 接口契约 + `mock.js` 数据结构）
> 版本：v0.1（初稿，待与负责数据库的同学确认）
> ⚠️ **已更新**：告警阈值已改为**每地块一行**（`plot_threshold`），替代原全局单行 `threshold`。以 `03_数据库建表SQL.sql` 与 `04_数据库设计说明文档.md` 为准。

---

## 一、从前端反推数据需求

前端所有页面都走 `window.API`，接口和数据形状已定死（`frontend/assets/js/api.js` + `mock.js`）：

| 前端接口 | 返回的数据形状 | 需要的表 |
|---------|--------------|---------|
| `API.getPlots()` 地块列表 | `{id,name,crop,area,temp,humidity,deviceCount,onlineCount}` | `plot` + 聚合 |
| `API.getDevices()` 设备列表 | `{id,name,type,plotId,plotName,online,controllable,running}` | `device`（join plot） |
| `API.getRealtime(plotId)` 实时温湿度 | `{plotId,plotName,temp,humidity,updatedAt}` | `sensor_data`（最新值） |
| `API.getHistory(plotId,7)` 历史趋势 | `{dates:[],temp:[],humidity:[]}` | `sensor_data`（按时间查） |
| `API.getThresholds()` / `saveThresholds()` 阈值 | `{humidityMin,tempMax}` | `plot_threshold`（每地块一行） |
| `API.getAlarms()` 告警列表 | `[{id,time,plotName,type,value,level,status}]` | `alarm`（join plot） |
| `API.updateAlarmStatus(id,status)` | `{code}` | `alarm` |
| `API.controlIrrigation(id,'on/off')` 灌溉控制 | `{code,data:device}` | `device.running` + `control_log` |
| `API.addDevice()` / `unbindDevice()` 设备管理 | `{code,data:device}` | `device` |
| `API.getChatReply()` 智能问答 | `{answer,action}` | RAG 知识库（不在 MySQL） |
| `API.login()` 登录 | `{token,name,roleName,role}` | `user` |
| `API.register({username,password,role})` 注册 | `{code,msg}` | `register_request`（待审核） |
| `GET /api/register/list`（后端待补） 待审核列表 | `[{id,username,role,status,createdAt}]` | `register_request` |
| `POST /api/register/review`（后端待补） 审核 | `{id,action,rejectReason?}` → `{code,msg}` | `register_request` + `user` |

> 设备控制页还有一份**控制日志**（`control.html` 目前直接读 `MOCK.controlLogs`），字段：`{id,time,deviceId,deviceName,plotName,action,result,operator}` —— 后端也应提供 `control_log` 表 + 对应接口。

---

## 二、表关系（ER 简图）

```
plot 1 ──── N device            （一个地块多个设备）
device 1 ── N sensor_data       （一个设备不断上报数据）
plot 1 ──── N alarm             （地块产生告警；离线告警关联 device_id）
device 1 ── N control_log       （灌溉操作留痕）
plot_threshold 每地块一行（plot 1 ── 1 plot_threshold）
register_request 1 ── 0..1 user   （审核通过后生成用户）

外键策略：不加物理外键，只建索引 + 应用层保证一致性（原因见第六节）
```

---

## 三、建表语句与字段说明

### 1. plot — 地块表

```sql
CREATE TABLE plot (
    id         VARCHAR(16)  PRIMARY KEY,       -- 地块编号，如 P001（前端 URL 要用它，必须稳定）
    name       VARCHAR(50)  NOT NULL,          -- 地块名称，如 "一号大棚"
    crop       VARCHAR(50)  NOT NULL,          -- 种植作物，如 "番茄"
    area       DECIMAL(8,2) NOT NULL,          -- 面积（亩），如 2.50
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
) COMMENT='地块表';
```

**要点**
- 前端传的 `plotId` 是 `P001` 这种**字符串**，所以主键用 `VARCHAR(16)`，不用自增 int
- `area` 存**数字**，显示时前端拼单位"亩"（mock 里 `'2.5亩'` 是显示格式，别存带单位的字符串）
- 列表里的 `temp/humidity/deviceCount/onlineCount` 是**聚合算出来的**，不存表里：
  - `deviceCount` = `COUNT(device WHERE plot_id=?)`
  - `onlineCount` = `COUNT(device WHERE plot_id=? AND online=1)`
  - `temp/humidity` = 该地块最新一条温/湿度读数

---

### 2. device — 设备表

```sql
CREATE TABLE device (
    id             VARCHAR(16)  PRIMARY KEY,   -- 设备编号，如 D001
    plot_id        VARCHAR(16)  NOT NULL,      -- 所属地块（关联 plot.id）
    name           VARCHAR(50)  NOT NULL,      -- 设备名称，如 "土壤湿度传感器-01"
    type           VARCHAR(20)  NOT NULL,      -- 土壤湿度 / 温度 / 灌溉设备
    online         TINYINT      DEFAULT 1,     -- 1在线 0离线（心跳超时置离线）
    running        TINYINT      DEFAULT 0,     -- 灌溉开关状态，仅灌溉设备用（0关 1开）
    last_heartbeat DATETIME     DEFAULT NULL,  -- 最后心跳时间（在线判断依据）
    created_at     DATETIME DEFAULT CURRENT_TIMESTAMP,
    KEY idx_plot (plot_id)
) COMMENT='设备表';
```

**要点**
- `plot_id` 关联地块；接口返回的 `plotName` 由后端 join 出来，不冗余存储
- `controllable`（是否可控制）**不存**，由 `type == '灌溉设备'` 推出
- `online` 由 `last_heartbeat` 判定：心跳超过 N 秒没来 → 置 0 + 触发离线告警

---

### 3. sensor_data — 传感器数据表（时间序列，写入最频繁）

```sql
CREATE TABLE sensor_data (
    id           BIGINT        AUTO_INCREMENT PRIMARY KEY,
    device_id    VARCHAR(16)   NOT NULL,       -- 哪个设备上报的（关联 device.id）
    metric       VARCHAR(20)   NOT NULL,       -- 指标：temp 温度 / humidity 湿度
    value        DECIMAL(8,2)  NOT NULL,       -- 数值
    collected_at DATETIME      NOT NULL,       -- 采集时间
    KEY idx_dev_metric_time (device_id, metric, collected_at)
) COMMENT='传感器数据表';
```

**要点**
- 一张表装两种指标（温度、湿度），靠 `metric` 区分 —— 以后加光照/土壤pH等指标不用改表
- 历史趋势查询：`WHERE device_id=? AND metric='temp' AND collected_at BETWEEN ? AND ?`
- 地块级历史：join device 拿 `plot_id`，再按 metric 聚合
- 实时值：每个 `(plot_id, metric)` 取 `collected_at` 最新一条
- ⚠️ 写入最频繁，`collected_at` 建议 `DATETIME(3)`（毫秒）；数据量大后要按时间归档/清理

---

### 4. plot_threshold — 告警阈值配置表（每地块一行）

```sql
CREATE TABLE plot_threshold (
    plot_id      VARCHAR(16)  PRIMARY KEY,   -- 地块编号（关联 plot.id），每地块一行
    humidity_min DECIMAL(5,2) DEFAULT 40,    -- 土壤湿度下限（%）
    temp_max     DECIMAL(5,2) DEFAULT 35,    -- 温度上限（℃）
    updated_at   DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) COMMENT='告警阈值配置表（每地块一行）';
```

**要点**
- 每个地块一条阈值记录，主键即 `plot_id`（与 `plot` 1:1）
- 未配置阈值的地块，后端判定时使用默认值（湿度 40%、温度 35℃）

---

### 5. alarm — 告警表

```sql
CREATE TABLE alarm (
    id         BIGINT       AUTO_INCREMENT PRIMARY KEY,
    plot_id    VARCHAR(16)  NOT NULL,          -- 关联 plot.id
    device_id  VARCHAR(16)  DEFAULT NULL,      -- 触发设备（设备离线告警时填）
    alarm_type VARCHAR(30)  NOT NULL,          -- 土壤湿度过低 / 温度过高 / 设备离线
    value      VARCHAR(20)  DEFAULT NULL,      -- 触发时的值，如 '38%'、'36.5℃'、'-'
    level      VARCHAR(10)  NOT NULL,          -- 严重 / 警告
    status     VARCHAR(10)  DEFAULT '未处理',   -- 未处理 / 已处理
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    handled_at DATETIME     DEFAULT NULL,
    handler    VARCHAR(50)  DEFAULT NULL,      -- 处理人
    KEY idx_plot_time (plot_id, created_at)
) COMMENT='告警表';
```

**要点**
- `alarm_type / level / status` 直接存前端要显示的**中文值**（和 mock 一致），后端判断阈值时写库，前端原样展示，省一层映射
- `value` 是展示型字段（带单位），存字符串即可
- 阈值告警：`sensor_data` 新值低于/高于该地块的 `plot_threshold` → 插一条 `alarm`
- 离线告警：心跳超时 → 插一条 `alarm(device_id=..., alarm_type='设备离线')`

---

### 6. control_log — 灌溉控制日志表

```sql
CREATE TABLE control_log (
    id         BIGINT       AUTO_INCREMENT PRIMARY KEY,
    device_id  VARCHAR(16)  NOT NULL,          -- 关联 device.id
    action     VARCHAR(10)  NOT NULL,          -- 开启 / 关闭
    result     VARCHAR(10)  NOT NULL,          -- 成功 / 失败
    operator   VARCHAR(50)  DEFAULT NULL,      -- 操作人，如 "农户·张老三"
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    KEY idx_device_time (device_id, created_at)
) COMMENT='灌溉控制日志表';
```

**要点**
- `deviceName` / `plotName` 展示时 join 出来，不冗余存储
- 每次 `controlIrrigation` 时插一条，实现控制留痕

---

### 7. user — 用户表（登录用）

```sql
CREATE TABLE user (
    id         INT          AUTO_INCREMENT PRIMARY KEY,
    username   VARCHAR(50)  NOT NULL UNIQUE,
    password   VARCHAR(100) NOT NULL,          -- 存哈希（如 BCrypt），别存明文！
    name       VARCHAR(50)  NOT NULL,          -- 显示名，如 "张老三"
    role       VARCHAR(20)  NOT NULL,          -- farmer 农户 / admin 农场管理员 / sysadmin 系统管理员
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
) COMMENT='用户表';
```

**要点**
- 前端 `roleName`（农户/农场管理员/系统管理员）由 `role` 映射，不存
- `token` 由后端登录时签发（JWT/会话），不一定要存库

---

### 8. register_request — 注册申请审核表

```sql
CREATE TABLE register_request (
    id            BIGINT       AUTO_INCREMENT PRIMARY KEY,
    username      VARCHAR(50)  NOT NULL,          -- 申请注册的账号
    password      VARCHAR(100) NOT NULL,          -- 密码哈希，审核通过后写入 user
    role          VARCHAR(20)  NOT NULL,          -- 申请角色（用户自选）：farmer/admin/sysadmin
    status        VARCHAR(10)  NOT NULL DEFAULT '待审核',  -- 待审核 / 已通过 / 已拒绝
    reject_reason VARCHAR(200) DEFAULT NULL,      -- 拒绝原因
    created_at    DATETIME DEFAULT CURRENT_TIMESTAMP,
    reviewed_at   DATETIME     DEFAULT NULL,      -- 审核时间
    reviewer      VARCHAR(50)  DEFAULT NULL,      -- 审核人
    user_id       BIGINT       DEFAULT NULL,      -- 审核通过后关联生成的 user.id
    KEY idx_status (status),
    KEY idx_username (username)
) COMMENT='注册申请审核表';
```

**要点**
- 注册不直接写 `user`，先落 `register_request` 待审核，管理员审核通过后再写 `user`
- `password` 全程存哈希；`user_id` 通过后回填，用于追溯
- 前端注册表单提交 `username` + `password` + `role`（用户自选身份），审核通过后 `role` 原样写入 `user`

---

## 四、接口返回里的"聚合字段"都是算出来的

| 返回字段 | 怎么算 |
|---------|--------|
| `plot.deviceCount` | `COUNT(*) FROM device WHERE plot_id=?` |
| `plot.onlineCount` | `COUNT(*) FROM device WHERE plot_id=? AND online=1` |
| `plot.temp` / `plot.humidity` | 该地块最新一条 `sensor_data`（按 metric） |
| `device.controllable` | `type == '灌溉设备'` |
| `device.plotName` | join `plot.name` |
| 实时/历史温湿度 | `sensor_data` 按 `(plot, metric, time)` 查 |

这些**不需要建额外表/字段**，后端查询时算。

---

## 五、团队统一约定

1. **主键风格**：`plot.id`、`device.id` 用业务字符串（`P001`/`D001`，前端 URL 要用）；`alarm`/`sensor_data`/`control_log`/`register_request` 用自增 `BIGINT`
2. **时间戳**：统一 `DATETIME`；高频的 `sensor_data.collected_at` 用 `DATETIME(3)`
3. **命名**：表名/字段全小写下划线（`snake_case`），代码里保持一致
4. **中文展示字段**（`alarm.alarm_type/level/status`、`control_log.action/result`）直接存中文，与前端 mock 一致
5. **不加物理外键**：高频写入 + 多人分工时外键有开销、易锁表，用索引 + 应用层保证
6. **正式应用库名**：建在专门库（如 `smoke` 之外的 `farm` 或同学定的名字），**不要用 `sys` 系统库**
