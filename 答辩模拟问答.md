# 智慧农业平台答辩模拟问答

> 使用建议：先背“一分钟总介绍”和“关键事实速查”，再按自己分工准备对应问题。答辩时优先说当前代码真实实现，不要把早期文档里的规划说成已经完整上线。

## 一、一分钟总介绍

我们项目是一个面向大棚/地块管理的智慧农业平台，核心目标是把传感器采集、设备控制、告警处理、视频监控、智能问答和巡田展示统一到一个 Web 系统里。硬件侧使用 Hi3861/BearPi 类开发板连接 E53_IA1 传感器，通过 WiFi 和 Java 后端建立 TCP 长连接，上传温度、湿度、光照数据。后端把数据写入 MySQL，并提供实时监测、历史趋势、阈值告警、灌溉控制、用户登录注册审核、摄像头代理、人体识别记录、RAG 智能问答等 API。前端用原生 HTML/CSS/JavaScript 实现专业版页面，同时提供游戏化 PVZ 风格页面方便演示和科普。

当前真实链路可以概括为：

```text
E53_IA1 传感器 -> Hi3861/BearPi 开发板 -> WiFi -> TCP 长连接
-> Java BoardCollector -> MySQL -> Java API -> 前端页面
```

## 二、关键事实速查

| 老师可能问 | 建议直接回答 |
|---|---|
| 项目现在用 MQTT 吗 | 当前代码真实硬件链路是 TCP 长连接。文档里 MQTT 是早期设计表述，当前版本尚未把 MQTT 作为板端链路使用。 |
| 设备上传哪些信号 | 温度 `temp`、湿度 `humidity`、光照 `lux`。硬件 E53_IA1 的 SHT30 实际测的是环境相对湿度，页面里“土壤湿度”是原型阶段的湿度代理。 |
| 心跳是什么 | 板端每 5 秒推送 `HEARTBEAT TEMP:xx HUMI:xx LUX:xx`，证明设备存活，同时刷新传感器数据和在线状态。 |
| 几张数据库表 | 完整导出是 14 张表；其中 8 张是核心农业业务表，另外 6 张支持智能问答和人体识别。 |
| JWT 是什么，项目用了没 | JWT 是由 Header、Payload、Signature 组成的无状态身份令牌。项目当前只返回 `token-role-随机串` 的演示 token，没有完整 JWT 签发、验签和过期机制。 |
| 密码怎么存 | 当前代码用 SHA-256 保存密码摘要，演示账号密码为 `123456`。生产环境应改成带盐 BCrypt、PBKDF2 或 Argon2。 |
| 后端框架 | 不是 Spring Boot，也不是 Node 项目。后端主要是 Java + JDK HttpServer/PlainWebServer + JDBC + MySQL。 |
| 前端怎么切真实/模拟 | `frontend/assets/js/api.js` 默认 `useMock: true`；访问 `?simulator=0` 或顶栏切换可走真实后端。 |
| 无人机真实吗 | 当前完成巡田地图、无人机动画、绑定地块、模拟巡航和自动灌溉交互；真实无人机遥测表 `drone_status` 尚未接入。 |
| RAG 是向量数据库吗 | 当前不是向量数据库，是轻量词法检索：中文 bigram、关键词、同义词、IDF 加权，取 Top-K 知识块拼给 DeepSeek。 |

## 三、按小组分工模拟提问

### 1. 隋元皓：组长、架构实现、后端数据库设计、无人机、PVZ 初版、创新点

**老师可能问：你作为组长，项目整体架构是什么？**

参考回答：项目分为硬件采集层、Java 服务层、MySQL 数据层和 Web 展示层。硬件采集温湿度和光照，经 TCP 长连接传到 Java 后端；后端负责采集入库、设备状态、告警、登录注册、智能问答、摄像头代理等；MySQL 存业务数据和历史数据；前端展示数据总览、监测、历史、控制、告警、地图、视频、问答和游戏化页面。

老师继续追问时：可以强调后端没有使用 Spring Boot，而是用 JDK 自带 HTTP 服务和 JDBC，优点是部署轻量、课程演示依赖少。

**老师可能问：为什么数据库设计成这些表？完整有几张？**

参考回答：完整项目导出有 14 张表。核心业务 8 张：`user`、`plot`、`device`、`sensor_data`、`plot_threshold`、`alarm`、`control_log`、`register_request`。扩展能力 6 张：`conversation`、`chat_message`、`knowledge_chunk`、`human_detection_record`、`camera_detection_setting`、`camera_detection_runtime`。这样把用户、地块、设备、传感器时序数据、阈值、告警、控制日志、注册审核拆开，既便于查询，也便于后续扩展。

老师继续追问时：文档中如果出现 7 张或 8 张，是早期核心业务口径；以当前 `mysql.sql` 完整导出为准是 14 张。

**老师可能问：项目的创新点有哪些，哪些是真实实现，哪些是规划？**

参考回答：真实实现的创新点包括真实开发板数据接入、专业版/游戏版双界面、RAG 智能问答、摄像头视频代理、YOLO 人体识别记录和告警联动、飞书机器人告警/查询、重庆巡田地图和无人机巡航模拟。预测性灌溉目前有后端 GRU 风格推理器和规则层，但它是固定权重加趋势校准的课程原型，不是大规模训练后的生产模型；动态灌溉、异常检测等更高级能力属于后续增强方向。

**老师可能问：无人机模块的数据从哪里来？**

参考回答：当前地图页的无人机、位置、巡航路线、绑定地块和电量主要保存在前端状态和 `localStorage`，并结合地块、设备、告警、实时数据做展示和交互。真实无人机遥测链路尚未接入数据库，飞书真实模式下也会提示 `drone_status` 表尚未接入。

老师继续追问时：后续可以增加 `drone_status` 表，字段包括 `drone_id`、`plot_id`、`lat`、`lng`、`battery`、`task_status`、`updated_at`，再接入真实无人机定位或遥测接口。

**老师可能问：为什么要做游戏版？**

参考回答：专业版面向实际管理，强调数据、告警和设备控制；游戏版把地块、设备、告警、摄像头、灌溉包装成更直观的交互形式，降低演示和科普门槛。它不是替代专业版，而是同一套业务能力的可视化表达。

### 2. 刘江渝：硬件、板端连接、传感器采集、TCP 通信、视频监控和人体识别

**老师可能问：开发板怎么联网，怎么和后端通信？**

参考回答：开发板连接 WiFi 后作为 TCP 服务端监听 `8888` 端口，Java 后端的 `BoardCollector` 作为 TCP 客户端连接开发板 IP 和端口。连接建立后，后端发送 `query` 获取一次数据，开发板会返回 `DATA TEMP:xx HUMI:xx LUX:xx`，之后每 5 秒主动推送 `HEARTBEAT`。

**老师可能问：设备上传了哪些信号？单位是什么？**

参考回答：当前上传三个信号：温度、湿度、光照。温度单位是摄氏度，湿度单位是百分比，光照单位是 lux/lx。数据库 `sensor_data.metric` 使用 `temp`、`humidity`、`lux` 三个指标名。

老师继续追问时：E53_IA1 的 SHT30 实际测量环境相对湿度，BH1750 测量光照。页面里叫土壤湿度，是原型阶段沿用的业务命名；严格来说后续要接入土壤湿度传感器才能称为真实土壤含水率。

**老师可能问：心跳是什么？为什么需要心跳？**

参考回答：心跳是设备周期性发送的一条存活消息。我们板端每 5 秒发送一次 `HEARTBEAT TEMP:xx HUMI:xx LUX:xx`，它既证明连接还活着，也顺带上传最新环境数据。后端收到后写库、更新 `last_heartbeat` 和在线状态。如果约 25 秒没有新数据，后端会把设备判为离线。

**老师可能问：TCP 和 MQTT 有什么区别？你们为什么当前用 TCP？**

参考回答：TCP 是传输层连接协议，开发者需要自己定义消息格式、心跳、重连和命令确认；MQTT 是应用层消息协议，基于发布/订阅模型，有主题、QoS、Broker 等机制。我们当前硬件端实现简单，使用 TCP 长连接就能完成数据上传和 on/off 控制，部署也少一个 Broker。后续如果设备数量变多，可以迁移到 MQTT。

**老师可能问：板子 IP 改了怎么办？**

参考回答：设备表里保存每个设备的 `ip` 和 `port`。前端设备管理页可以修改 IP/端口，后端更新后会重置在线状态，并让 `BoardCollector` 立即重新扫描连接。

**老师可能问：视频监控和人体识别怎么做？**

参考回答：摄像头设备保存 IP、端口、协议和账号密码。前端可以直连 MJPEG/HTTP，也可以走后端同源代理，避免浏览器跨域和暴露账号密码。人体识别是一个可选 Python worker，使用 OpenCV 读取摄像头流，用 YOLO 检测 `person` 类，达到置信度阈值后保存截图和 MP4，并调用 Java 内部 API 写入 `human_detection_record` 和严重告警。

### 3. 赵虹尊：后端服务器、前端接口、基本功能、数据库对接

**老师可能问：Java 服务启动后做了哪些初始化？**

参考回答：`WebServer` 启动后会托管 `frontend` 静态页面，优先处理 `/api/*` 业务接口；然后确保对话表、知识库表、阈值字段、摄像头字段、地图字段、告警处理字段、人体识别表存在；再初始化 RAG 知识库，启动 `BoardCollector` 板端采集线程，最后尝试启动飞书机器人。

**老师可能问：你们有哪些主要 API？**

参考回答：主要有地块 `/api/plots`，设备 `/api/devices`，实时数据 `/api/plots/{id}/realtime`，历史数据 `/api/plots/{id}/history`，灌溉控制 `/api/devices/{id}/control`，阈值 `/api/thresholds`，告警 `/api/alarms`，登录注册 `/api/auth/login` 和 `/api/auth/register`，注册审核 `/api/register-requests`，智能问答 `/api/assistant/chat`，天气 `/api/weather`，摄像头和人体识别 `/api/camera/*`。

**老师可能问：前端和后端如何对接？**

参考回答：前端统一通过 `frontend/assets/js/api.js` 的 `window.API` 调接口，不在页面里直接散写 `fetch`。这个共享层定义了后端路径和数据形状，也提供模拟模式。真实模式时请求 Java API，模拟模式时使用 `mock.js` 的本地数据。

**老师可能问：历史趋势怎么查？**

参考回答：后端按地块查询 `sensor_data`，关联 `device` 过滤所属地块和设备类型。支持 `days=N` 按日聚合，也支持 `window=1m/30m/24h` 做短时窗口聚合，返回 `dates`、`temp`、`humidity`、`lux` 三组序列给 ECharts。

**老师可能问：如何避免 SQL 注入？**

参考回答：后端数据库操作基本使用 `PreparedStatement`，参数通过占位符传入，不把用户输入直接拼进 SQL。像地块 ID、设备 ID、注册用户名、阈值等都走参数绑定。少数动态 SQL 只拼固定枚举、limit 或已校验字段。

**老师可能问：接口失败时怎么返回？**

参考回答：所有业务接口统一返回 JSON，成功是 `{"code":0,"data":...}`，失败是 `{"code":1,"msg":"..."}`。数据库异常会返回数据库错误，未知异常会返回服务器内部错误，前端据此弹提示或降级。

### 4. 张创举：后端辅助、基础功能测试、PVZ 游戏端、真实/模拟切换

**老师可能问：专业版和游戏版有什么区别？**

参考回答：专业版是实际管理界面，包括数据总览、监测、历史、设备、告警、问答等；游戏版是把真实业务对象映射到 PVZ 风格交互中，比如用一行表示一个地块，用植物卡片表示灌溉、摄像头、农事建议、告警处理等动作，用敌方单位表现告警压力。它提高展示性，但底层仍调用同一套 `window.API`。

**老师可能问：游戏版里灌溉是真的控制设备吗？**

参考回答：在模拟模式下，游戏版只改本地 mock 状态；在真实模式下，它会调用 `/api/devices/{deviceId}/control`，由后端通过 TCP 长连接向板子发送 `on/off`。所以游戏版不是纯动画，它可以作为真实控制入口，但答辩演示时要注意当前处于模拟还是真实模式。

**老师可能问：你们为什么设计模拟模式？**

参考回答：模拟模式解决三类问题：一是答辩现场没有开发板或数据库也能演示完整流程；二是前端开发不依赖后端实时可用；三是便于测试登录、告警、控制、摄像头等交互。真实模式通过 `?simulator=0` 切换，模拟和真实复用同一套 API 契约。

**老师可能问：怎么测试真实数据和模拟数据切换？**

参考回答：看顶栏模式提示。模拟模式会显示“不连接后端”，并阻断真实请求；真实模式下页面会请求 Java API 和 MySQL。可以用设备管理、实时监测、历史趋势、告警页面验证：模拟模式数据来自 `mock.js`，真实模式数据来自数据库和板端采集。

**老师可能问：PVZ 游戏逻辑如何和农业业务对应？**

参考回答：地块对应草坪行，设备状态对应可放置的功能卡片，灌溉设备对应开启/关闭动作，摄像头对应查看画面，农事建议对应智能问答或规则建议，告警对应需要处理的事件。这样把农业监控的“看数据、做决策、处理异常”变成可交互流程。

### 5. 杨泓：专业版前端、告警管理、设备控制、阈值、日志、ECharts 可视化

**老师可能问：专业版前端有哪些页面？**

参考回答：主要页面有登录、数据总览、数据监测、历史趋势、重庆巡田、视频监控、设备控制、告警管理、智能问答、设备管理、注册审核和游戏版入口。布局由 `layout.js` 统一注入侧边栏和顶栏。

**老师可能问：告警管理怎么触发？**

参考回答：传感器数据写入后，后端查询地块阈值和该地块最新 `temp/humidity/lux`，判断是否超过上下限。如果一个指标越界，生成具体告警；如果多个指标同时越界，生成综合阈值告警。温度过高或多指标越界会作为严重告警，其他一般是警告。

**老师可能问：如何避免告警刷屏？**

参考回答：后端有 60 秒短时间去重。对同一地块、同一告警类型，如果 60 秒内已有未处理告警，就不重复插入。这样既保留异常记录，又避免心跳频繁上传导致告警过多。

**老师可能问：告警处理流程是什么？**

参考回答：前端调用 `/api/alarms/{id}` 把状态改为“已处理”，后端要求填写处理日志，同时记录处理人和处理时间。若告警关联人体识别记录，会同步把 `human_detection_record.status` 改为已处理。也支持按地块一键处理未处理告警。

**老师可能问：阈值怎么设计？**

参考回答：`plot_threshold` 按地块保存阈值，字段包括湿度下限/上限、温度下限/上限、光照下限/上限。默认口径可回答湿度 40-70、温度 10-35、光照 200-800。当前数据库里允许某些阈值为空，表示禁用对应指标。

**老师可能问：历史数据可视化用什么？**

参考回答：前端使用 ECharts 展示温度、湿度、光照趋势。后端返回三条对齐的时间序列，缺失值用 `null` 表示，图表会显示断点，避免用错误值填充。

### 6. 谢明成：多用户登录、注册管理、角色权限、登录态

**老师可能问：系统有哪些角色？**

参考回答：有三种角色：农户 `farmer`、农场管理员 `admin`、系统管理员 `sysadmin`。农户可以看总览、监测、历史、地图、视频、控制、告警和问答；管理员和系统管理员还可以进入设备管理和注册审核。

**老师可能问：登录流程是什么？**

参考回答：前端提交用户名和密码到 `/api/auth/login`。后端查询 `user` 表，用 SHA-256 校验密码摘要，成功后返回用户名、显示名、角色和一个演示 token。前端把用户信息保存到 `localStorage`，`layout.js` 根据角色渲染导航和控制页面访问。

**老师可能问：JWT 是什么？你们这个 token 是 JWT 吗？**

参考回答：JWT 是 JSON Web Token，由 Header、Payload、Signature 三部分组成，服务端签名后客户端携带，服务端可以无状态验签识别用户。我们当前返回的是 `token-role-随机串` 演示 token，不是真正 JWT；后端也没有统一验签中间件。正式版应该实现 JWT 签发、过期、刷新和后端统一鉴权。

**老师可能问：注册为什么要审核？**

参考回答：农业平台涉及设备控制和告警处理，不能让任何注册用户直接操作设备。注册时先写入 `register_request`，状态为“待审核”；管理员审核通过后才写入 `user` 表，拒绝则记录拒绝原因。

**老师可能问：只在前端控制权限安全吗？**

参考回答：当前课程原型主要在前端做页面访问控制，部分接口也传角色做限制，但还不是完整安全模型。生产环境必须在后端统一做权限校验，不能只依赖前端隐藏菜单。

## 四、老师高概率综合问题

**老师可能问：这个项目解决了什么实际问题？**

参考回答：解决农业管理中数据分散、人工巡检成本高、异常发现慢、设备控制不直观的问题。平台把实时环境、历史趋势、灌溉控制、告警处理、摄像头监控和智能问答整合在一起，让管理者能及时发现问题并采取措施。

**老师可能问：从传感器到页面展示，数据完整流程是什么？**

参考回答：传感器采集温度、湿度、光照；开发板通过 TCP 把 `DATA/HEARTBEAT` 发给 Java 后端；`BoardCollector` 解析后写入 `sensor_data`，并更新设备在线状态；API 查询最新值或历史聚合；前端通过 `api.js` 获取数据并展示在总览、监测、历史趋势、地图、游戏等页面。

**老师可能问：为什么一个开发板对应多个逻辑设备？**

参考回答：硬件上一块 E53_IA1 同时采集温度、湿度、光照，还能接灌溉控制。业务上为了页面和数据库表达清楚，把温度传感器、湿度传感器、亮度传感器、灌溉设备建成不同逻辑设备，但它们可以共享同一个板子的 IP 和端口。后端按 `<ip:port>` 去重，一块板子只维护一条 TCP 长连接。

**老师可能问：如何判断设备在线/离线？**

参考回答：普通板载设备看 TCP 连接和最近传感器数据新鲜度。收到数据后更新 `online=1` 和 `last_heartbeat`；如果约 25 秒没有新数据，就判为离线。摄像头则通过 HTTP/MJPEG 请求或 RTSP 端口探测刷新在线状态。

**老师可能问：灌溉控制如何确认成功？**

参考回答：业务设计上，后端通过 TCP 长连接发送 `on/off`，板端会返回 `motor started/motor stopped`，并写控制日志。需要特别诚实的是：当前 `BoardCollector` 代码层面主要以命令成功写入长连接作为接口成功条件，`motor` 回包已解析但没有和某个请求严格绑定等待。后续改进是让 `CommandRequest` 等到对应 `motor started/stopped` 再返回成功。

**老师可能问：为什么不用物理外键？**

参考回答：课程项目为了便于导入导出、反复初始化、演示数据迁移和模块迭代，主要用应用层关联和索引，没有强制物理外键。比如 `device.plot_id`、`sensor_data.device_id`、`alarm.plot_id` 都通过业务代码维护一致性。生产环境可以补充外键或软删除策略。

**老师可能问：为什么传感器数据单独建 `sensor_data` 表？**

参考回答：传感器数据是高频时序数据，增长速度远高于地块和设备基础信息。单独建表可以按设备、指标、时间查询和索引，也便于后续扩展更多指标，例如 pH、CO2、电导率，不需要每加一个指标就改设备表字段。

**老师可能问：天气接口怎么做？**

参考回答：后端接入和风天气，配置 `QWEATHER_API_KEY` 后调用实时天气和 7 天预报接口；未配置时返回 `code=1`，前端降级到模拟数据。答辩时不要展示真实 API Key。

**老师可能问：飞书机器人有什么用？**

参考回答：飞书机器人是可选模块，可以推送告警，也支持命令查询，例如地块状态、设备状态、今日告警、无人机位置、今日总结。未配置飞书密钥时不会影响本地 Web 服务。

## 五、基础概念快问快答

**心跳是什么？**

心跳是设备周期性发送的存活包。我们这里每 5 秒发送一次带温湿度光照的 `HEARTBEAT`，既检测在线，也上传数据。

**JWT 是什么？**

JWT 是一种 JSON 格式的身份令牌，包含 Header、Payload、Signature。签名用于防篡改，常用于前后端分离的无状态登录。

**RAG 是什么？**

RAG 是检索增强生成。先从知识库检索相关资料，再把资料放进大模型上下文中，让回答更贴合项目知识和农业知识。

**GRU 是什么？**

GRU 是门控循环单元，是一种循环神经网络结构，适合时间序列预测。项目里有 1 层 GRU 风格的湿度预测逻辑，但当前是固定权重加趋势校准的原型。

**YOLO 是什么？**

YOLO 是目标检测算法，可以在图片或视频帧中检测目标类别和位置。项目 worker 使用 YOLO 检测人形目标，产生人体入侵记录和严重告警。

**TCP 为什么要重连？**

WiFi 或开发板可能断开，后端如果不重连就无法继续采集。`BoardCollector` 断线后约 5 秒重连，后台每 30 秒扫描设备表。

## 六、数据库专项问题

**完整 14 张表是什么？**

1. `alarm`：告警表。
2. `camera_detection_runtime`：人体识别 worker 运行状态。
3. `camera_detection_setting`：人体识别参数。
4. `chat_message`：智能问答消息。
5. `control_log`：设备控制日志。
6. `conversation`：智能问答会话。
7. `device`：设备表。
8. `human_detection_record`：人体识别记录。
9. `knowledge_chunk`：RAG 知识库。
10. `plot`：地块表。
11. `plot_threshold`：地块阈值表。
12. `register_request`：注册申请表。
13. `sensor_data`：传感器数据表。
14. `user`：用户表。

**核心业务 8 张表是什么？**

`user`、`plot`、`device`、`sensor_data`、`plot_threshold`、`alarm`、`control_log`、`register_request`。

**`sensor_data` 为什么用 metric/value 而不是 temp/humidity/lux 三列？**

因为 metric/value 更容易扩展。现在只有 `temp`、`humidity`、`lux`，后续增加 pH、CO2、土壤电导率等指标时，只需要新增 metric 值，不一定要改表结构。

**`plot_threshold` 为什么按地块存？**

不同作物和地块环境不同，阈值不能完全一样。比如番茄、西瓜、叶菜对温度、湿度、光照要求不同，按地块存可以针对性配置。

**用户表和注册申请表为什么分开？**

`register_request` 表示还没有成为正式用户的申请，管理员审核通过后才写入 `user`。这样避免未审核用户直接登录和控制设备。

## 七、硬件与通信专项问题

**板端支持哪些命令？**

```text
query -> DATA TEMP:xx HUMI:xx LUX:xx
ping  -> PONG
on    -> motor started
off   -> motor stopped
每 5 秒 -> HEARTBEAT TEMP:xx HUMI:xx LUX:xx
```

**为什么用 NUL 字节结尾？**

板端数据是一条条记录，NUL 字节可以作为记录边界。后端读 socket 时遇到 `0x00` 就认为一条记录结束，再解析 `DATA` 或 `HEARTBEAT`。

**数据写库为什么放到线程池？**

每次心跳要给同一板子关联的多个逻辑设备写三条数据。如果在 socket 读取线程里同步写库，可能阻塞读取和命令处理。所以后端用后台写库线程池，读循环先快速解析和更新内存状态。

**如果开发板离线，页面会怎样？**

后端会把设备标为离线，实时数据可能显示空值或旧值，设备控制会返回失败。模拟模式仍可以演示页面流程，但真实硬件数据不会更新。

## 八、前后端与部署专项问题

**项目如何部署？**

在 Windows 项目根目录运行 `deploy.ps1`。基础部署需要 Java 服务端、`frontend`、`mysql.sql`、MySQL JDBC 驱动、JDK 17 和 MySQL。首次初始化数据库可使用脚本导入 `mysql.sql`，日常启动不需要重复初始化。

**为什么部署文档强调不要提交密钥？**

因为 `.env.local`、`config/feishu.local.properties` 等可能包含数据库密码、DeepSeek Key、飞书密钥。答辩展示时不要打开这些文件，更不要把真实密钥写进 PPT 或 Markdown。

**为什么有 `WebServer` 和 `PlainWebServer` 两个入口？**

`WebServer` 基于 JDK `HttpServer`，结构清晰；`PlainWebServer` 是基于阻塞式 `ServerSocket` 的 Windows 回退入口，部署脚本使用它以提高环境兼容性。两者都能托管页面并处理 API。

**前端如何做权限控制？**

登录后用户信息保存在 `localStorage`，`layout.js` 根据角色渲染菜单，并阻止无权限页面访问。当前这是课程原型做法，生产环境要把权限控制前移到后端统一校验。

## 九、智能问答、RAG、YOLO、无人机、PVZ 专项问题

**智能问答怎么结合实时数据？**

后端构造系统上下文时会加入地块、设备、告警、控制日志、最近传感器数据和 RAG 知识块。用户问“现在该浇水吗”时，回答既参考知识库，也参考当前湿度、阈值和灌溉设备状态。

**RAG 检索怎么做？**

启动时从 `knowledge_chunk` 加载知识块。检索时对中文做 bigram、停用词过滤、同义词扩展，并对标题、关键词、正文加权，使用 IDF 降低高频词影响，最后取 Top-K 知识块拼进 DeepSeek prompt。

**人体识别告警怎么生成？**

Python worker 读取摄像头流，YOLO 检测到人且置信度超过阈值后，先按冷却时间去重，再保存截图和视频片段，调用 Java 内部接口写 `human_detection_record`，同时插入 `alarm`，告警类型为“人体入侵”，级别为“严重”。

**为什么摄像头要走代理？**

浏览器直接访问摄像头容易遇到跨域、认证信息暴露、协议兼容问题。后端代理可以把摄像头流变成同源路径 `/api/camera/mjpeg` 或 `/api/camera/player`，前端访问更稳定，也避免把账号密码直接暴露在页面 URL 里。

**PVZ 页面和真实系统如何联动？**

游戏版调用同一套 `window.API`。模拟模式下操作 mock 数据，真实模式下可以触发真实 API，如灌溉控制、摄像头画面、告警处理、实时数据刷新。因此它是业务能力的游戏化展示层。

**预测性灌溉能不能说是 AI 模型？**

可以说它是 AI 风格的时间序列预测原型，但不要夸大。当前是 1 层 GRU 结构、固定权重、结合历史趋势和蒸发规则校准，没有经过大规模真实农田数据训练。正式上线需要采集更多连续数据并训练/验证模型。

## 十、项目不足与诚实答法

1. 当前硬件通信是 TCP，不是文档早期写的 MQTT。答辩要以代码真实实现为准。
2. 当前 token 不是完整 JWT，后端没有统一鉴权中间件。
3. 当前密码使用 SHA-256 摘要，生产环境应使用带盐慢哈希。
4. 当前 E53_IA1 测的是环境相对湿度，页面“土壤湿度”是原型口径。
5. 当前无人机是前端模拟巡航，真实无人机遥测链路尚未接入。
6. 当前预测性灌溉是原型推理，不是完整训练模型。
7. 当前灌溉控制接口主要确认命令写出成功，后续应严格等待板端 `motor started/stopped` 回包。
8. 当前权限主要依赖前端登录态和页面控制，生产环境必须后端统一鉴权。

遇到老师追问时可以这样回答：

> 我们这个版本是课程项目和演示原型，所以优先把“采集、入库、展示、控制、告警、问答、视频识别”的完整闭环打通。对安全认证、真实无人机、真实土壤湿度、模型训练和大规模设备接入，我们已经在设计上预留了扩展点，但不会把它们夸大成已经完全生产化。

## 十一、答辩演示流程建议

1. 登录系统，说明三种角色和注册审核。
2. 打开数据总览，说明地块、设备、实时环境和天气/建议。
3. 打开数据监测和历史趋势，说明 `sensor_data` 时序数据和 ECharts。
4. 打开设备管理，说明设备 IP/端口、同一板子多逻辑设备、真实/模拟模式。
5. 打开设备控制，演示灌溉开关和控制日志。
6. 打开告警管理，说明阈值、去重、处理日志和人体入侵告警。
7. 打开视频监控，说明摄像头代理、六路监控、YOLO 记录。
8. 打开智能问答，问“现在该浇水吗”或“预测未来 3 天是否需要灌溉”，说明 RAG 和实时数据上下文。
9. 打开重庆巡田，说明无人机当前是模拟巡航，后续可接真实遥测。
10. 打开游戏版，说明专业版和游戏版共用 API 契约。

## 十二、最容易被问倒的问题

**老师问：文档写 MQTT，你代码为什么是 TCP？**

答：MQTT 是早期设计方案，当前硬件固件已经实现的是 TCP 服务端。我们答辩以当前代码为准：Java 后端通过 TCP 长连接采集数据；如果后续设备规模扩大，会引入 MQTT Broker，把设备数据按主题发布。

**老师问：你们说 JWT，为什么代码里不是 JWT？**

答：这是当前版本的不足。我们实现了登录和角色返回，但 token 是演示 token，不是标准 JWT。正式版需要引入 JWT 库，生成带签名和过期时间的 token，并在所有需要权限的接口统一校验。

**老师问：你们说土壤湿度，传感器真的是土壤湿度吗？**

答：当前 E53_IA1 的 SHT30 是环境湿度传感器，不是真正插入土壤的含水率传感器。页面沿用了土壤湿度业务名，演示时相当于湿度代理；后续接入电容式土壤湿度或土壤水分传感器即可替换数据来源。

**老师问：如果没有网络、没有 API Key、没有开发板，还能演示吗？**

答：能。前端默认模拟模式，不连接后端也能展示主要流程。真实模式需要 Java 服务、MySQL、开发板和可选 API Key；DeepSeek、和风天气、飞书、YOLO worker 都是可选增强，缺失时不影响基础监测和页面演示。

**老师问：如何扩展到 pH、CO2、土壤电导率？**

答：硬件侧增加传感器采集和协议字段；后端解析后写入 `sensor_data` 新 metric；阈值表可新增对应上下限或改为通用阈值表；前端图表和告警页面增加指标配置即可。当前 `sensor_data(metric,value)` 的设计已经为多指标扩展做了准备。
