# 智慧农业平台部署文档

本文档适用于本项目当前版本（Windows 部署）。请严格按照顺序执行。所有命令都在项目根目录执行，项目根目录就是同时包含 `pom.xml`、`mysql.sql`、`src` 和 `frontend` 的目录。

## 1. 部署完成后的组成

项目由以下部分组成：

| 部分 | 目录或文件 | 是否为基础部署必需 |
|---|---|---|
| Java 服务端 | `src\server` | 必需 |
| 静态前端 | `frontend` | 必需 |
| 数据库完整导出 | `mysql.sql` | 首次初始化必需 |
| MySQL JDBC 驱动 | `lib\mysql-connector-j-8.0.33.jar` | 必需 |
| 项目 JDK | `jdk-17.0.2` | 推荐必需 |
| 开发板工程和固件 | `wifi-iot`、`Hi3861_wifiiot_app_allinone.bin` | 只有使用开发板时需要 |
| 人体识别 Worker | `workers`、`models`、`.venv-detection` | 可选 |
| 飞书机器人配置 | `config\feishu.local.properties` | 可选 |

项目不是 Spring Boot 项目，也不是 Node.js 项目。前端页面由 Java 服务直接托管，业务 API 由 Java 代码处理，数据库通过 MySQL JDBC 连接。

本项目提供两个 Java HTTP 入口：

- `server.WebServer`：基于 JDK `HttpServer`。
- `server.PlainWebServer`：基于阻塞式 `ServerSocket` 的回退入口。

`deploy.ps1` 使用 `server.PlainWebServer`，因为它不依赖 JDK `HttpServer` 的内部 selector 管道，在 Windows 环境下更稳定；页面、API、数据库初始化和开发板采集功能相同。

## 2. 重要安全提示

### 2.1 不要把密码和密钥提交到 Git
8
以下文件可能包含数据库密码、模型 API Key 或飞书密钥：

```text
.env.local
config\feishu.local.properties
config\feishu.runtime.properties
```

这些文件只保存在部署机上，不要上传到代码仓库、聊天记录或公开网盘。

### 2.2 `mysql.sql` 会覆盖数据库表

根目录的 `mysql.sql` 是一个约 7 MB 的完整 MySQL 导出文件，包含：

- `CREATE DATABASE farm`
- `USE farm`
- 14 张表的结构
- 现有演示数据、历史传感器数据、告警数据和知识库数据
- 14 条 `DROP TABLE IF EXISTS`

因此，执行下面的命令会删除并重建 `farm` 库中的项目表，原有数据会被覆盖：

```powershell
.\deploy.ps1 -InitializeDatabase -Force -BoardIp 10.94.204.29 -BoardPort 8888
```

只有首次部署、明确需要恢复这份完整备份，或已完成数据库备份时，才执行 `-InitializeDatabase`。日常启动只执行：

```powershell
.\deploy.ps1
```

## 3. 第一步：准备项目文件

### 3.1 复制完整项目

把整个项目目录复制到部署机，例如：

```text
D:\smoke
```

不要只复制 `target` 或 `frontend`。至少要保留以下路径：

```text
D:\smoke\pom.xml
D:\smoke\mysql.sql
D:\smoke\src
D:\smoke\frontend
D:\smoke\lib\mysql-connector-j-8.0.33.jar
D:\smoke\jdk-17.0.2\bin\java.exe
D:\smoke\jdk-17.0.2\bin\javac.exe
D:\smoke\deploy.ps1
```

项目路径可以不是 `D:\smoke`，但以后所有命令都必须在实际项目根目录执行。脚本会根据自身位置定位项目文件，不依赖固定盘符。

### 3.2 打开 PowerShell

按 `Win` 键搜索并打开 PowerShell，然后进入项目根目录：

```powershell
Set-Location D:\smoke
```

确认当前目录正确：

```powershell
Get-ChildItem pom.xml, mysql.sql, deploy.ps1, frontend\index.html
```

这四个文件都能列出时，说明目录正确。

### 3.3 允许当前窗口执行脚本

只对当前 PowerShell 窗口临时放宽执行策略，不修改系统永久策略：

```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
```

如果系统提示确认，输入 `Y`。

## 4. 第二步：准备 Java 和 Maven

### 4.1 使用项目自带 JDK

项目代码按 Java 11 源码级别编译，但完整运行环境需要 Java 11 或更高版本；本项目推荐直接使用自带 JDK 17：

```powershell
.\jdk-17.0.2\bin\java.exe -version
.\jdk-17.0.2\bin\javac.exe -version
```

输出版本应为 `17.0.2` 或同一 JDK 17 系列。

不要使用系统 Java 8 编译或运行完整服务。系统可能已经安装 Java 8，但它不能满足项目中使用的 Java 11 及以上 API。

如果项目没有 `jdk-17.0.2`，可以另外安装 JDK 17，并在启动时指定：

```powershell
.\deploy.ps1 -JavaHome "C:\Program Files\Java\jdk-17"
```

如果项目 JDK 不存在、`JAVA_HOME` 指向了错误目录，并且系统 PATH 中也没有可用的 JDK，脚本会自动从 Eclipse Adoptium 官方 API 获取 Windows x64 JDK 17，下载到：

```text
tools\jdk-download\
```

然后自动校验 SHA-256、解压并使用其中的 JDK。自动下载需要部署电脑可以访问互联网，下载包大约 180 MB 到 200 MB。官方手动下载地址：

```text
https://adoptium.net/temurin/releases/?version=17
```

自动下载期间命令行会显示获取地址、下载百分比、已下载大小、SHA-256 校验和解压进度；不要关闭 PowerShell 窗口。

如果下载失败或 JDK 校验失败，脚本会显示具体错误、写入项目根目录的 `deploy.error.log`，并等待按回车后再关闭窗口，避免通过快捷方式启动时窗口直接消失。

自动下载失败时，可以先手动下载并解压 JDK 17，再指定 JDK 根目录：

```powershell
.\deploy.ps1 -JavaHome "C:\Java\jdk-17"
```

`-JavaHome` 指向的目录必须同时包含：

```text
C:\Java\jdk-17\bin\java.exe
C:\Java\jdk-17\bin\javac.exe
```

如果不希望脚本访问网络下载 JDK，可以使用：

```powershell
.\deploy.ps1 -SkipJdkDownload
```

### 4.2 Maven

项目没有 Maven Wrapper。`deploy.ps1` 按以下顺序寻找 Maven：

1. 当前 `PATH` 中的 `mvn.cmd`
2. 项目目录 `tools\apache-maven-3.9.9\bin\mvn.cmd`
3. 如果仍未找到，自动从 Apache Maven 下载 Maven 3.9.9 到 `tools` 目录

首次构建需要访问 Maven 仓库下载依赖。项目依赖包括：

- `com.larksuite.oapi:oapi-sdk:2.4.19`
- `com.mysql:mysql-connector-j:8.0.33`
- 以上依赖的传递依赖

如果部署机不能访问公网，请提前安装 Maven 并准备好本机 Maven 仓库，然后使用：

```powershell
.\deploy.ps1 -MavenHome "C:\apache-maven-3.9.9"
```

## 5. 第三步：准备 MySQL

### 5.1 MySQL 服务要求

建议使用 MySQL 8.x，要求：

- MySQL 服务已经启动。
- 默认端口为 `3306`，若使用其他端口，要在 `.env.local` 中修改。
- Java 服务连接本机地址 `127.0.0.1`。
- 本机数据库账号为 `newuser`，密码为 `123456`。
- 字符集支持 `utf8mb4`。

`deploy.ps1` 检测到本机 MySQL/MariaDB 服务处于停止状态时，会自动尝试启动服务，并等待 `127.0.0.1:3306` 可用。

### 5.2 在本机安装 MySQL 8

如果 Java 服务和数据库部署在同一台 Windows 电脑上，推荐安装 MySQL Community Server 8.x。本节以默认安装目录 `C:\Program Files\MySQL\MySQL Server 8.0`、默认服务名 `MySQL80`、默认端口 `3306` 为例。

截至 2026 年 9 月 1 日，MySQL Installer 的当前 8.0 版本为 `8.0.46`。优先使用下面的官方下载页；页面会随版本更新，文档中的直链仅用于当前版本。

当 `.env.local` 中的 `DB_HOST` 是 `localhost`、`127.0.0.1`、`::1` 或本机计算机名，且脚本无法连接数据库端口时，`deploy.ps1` 会先检查本机是否存在 MySQL/MariaDB Windows 服务。未发现服务时，脚本会自动将 MySQL Installer 下载到 `tools\mysql-installer-web-community-8.0.46.0.msi`，校验 MD5 后以管理员权限打开安装向导。

自动流程不会替用户设置 `root` 密码，也不会自动导入项目数据库；完成向导后仍需按照 5.2.4 初始化 `farm` 库。若只想让脚本报告数据库错误而不下载，使用：

```powershell
.\deploy.ps1 -SkipMySqlInstallerDownload
```

```text
官方选择下载页：
https://dev.mysql.com/downloads/installer/

联网安装器（约 2 MB，安装过程中下载 MySQL Server）：
https://cdn.mysql.com/Downloads/MySQLInstaller/mysql-installer-web-community-8.0.46.0.msi

离线完整安装器（约 566 MB，适合目标机安装时不能联网）：
https://cdn.mysql.com/Downloads/MySQLInstaller/mysql-installer-community-8.0.46.0.msi
```

打开官方下载页后：

1. 在 `MySQL Installer` 页面选择 Windows 的 MSI Installer。
2. 部署机安装时可以联网，下载 `mysql-installer-web-community-8.0.46.0.msi`。
3. 部署机不能联网，下载 `mysql-installer-community-8.0.46.0.msi` 并拷贝到部署机。
4. 如果网页要求登录 Oracle 账号，点击页面下方的 `No thanks, just start my download.` 开始下载。

下载完成后可以校验文件完整性。官方页面当前列出的 MD5 分别为：

```powershell
# 联网安装器的预期 MD5：210420aef5b5006ab54bb1dab4183768
# 离线完整安装器的预期 MD5：270575e788f414c29a42e41203161727
Get-FileHash .\mysql-installer-web-community-8.0.46.0.msi -Algorithm MD5
```

#### 5.2.1 推荐：图形安装

双击下载的 MSI，以管理员身份运行，按以下选项配置：

1. `Setup Type` 选择 `Server only`。
2. 出现依赖项页面时点击 `Execute`，等待所有依赖安装完成。
3. `Type and Networking` 选择 `Development Computer`。
4. 勾选 `TCP/IP`，端口保持 `3306`。只供本机 Java 服务使用时，不勾选 `Open Windows Firewall ports for network access`。
5. `Authentication Method` 保持默认的 `Use Strong Password Encryption`。
6. 为 `root` 设置高强度密码；不要留空，也不要使用示例密码。
7. `Windows Service` 保持服务名 `MySQL80`，勾选 `Start the MySQL Server at System Startup` 和 `Standard System Account`。
8. 在 `Apply Configuration` 页面点击 `Execute`，所有步骤显示成功后点击 `Finish`。

如果 `3306` 已被占用，先查找占用者：

```powershell
Get-NetTCPConnection -LocalPort 3306 -State Listen -ErrorAction SilentlyContinue |
  Select-Object LocalAddress, LocalPort, OwningProcess
```

可以停止冲突服务，或改用未占用端口；改端口后必须在 `.env.local` 中同步设置 `DB_PORT`。

#### 5.2.2 命令行下载和安装

可以通过命令行安装。MySQL 官方的 `MySQLInstallerConsole.exe` 支持安装、配置和 `--silent` 无提示模式。对于本项目，推荐使用“命令行下载 + 控制台交互配置”：不需要把 `root` 密码写进命令历史或进程参数。

以管理员身份启动 PowerShell，并确认当前窗口已经提升权限：

```powershell
$currentUser = [Security.Principal.WindowsIdentity]::GetCurrent()
$principal = New-Object Security.Principal.WindowsPrincipal($currentUser)
if (-not $principal.IsInRole([Security.Principal.WindowsBuiltInRole]::Administrator)) {
    throw "请以管理员身份重新打开 PowerShell 后再执行安装。"
}
```

联网下载安装器并静默安装 MySQL Installer 本身：

```powershell
$version = "8.0.46.0"
$installer = Join-Path $env:TEMP "mysql-installer-web-community-$version.msi"
$url = "https://cdn.mysql.com/Downloads/MySQLInstaller/mysql-installer-web-community-$version.msi"

Invoke-WebRequest -Uri $url -OutFile $installer
if ((Get-FileHash $installer -Algorithm MD5).Hash -ne "210420AEF5B5006AB54BB1DAB4183768") {
    throw "MySQL Installer 文件校验失败，请删除下载文件后重新下载。"
}

$msiProcess = Start-Process msiexec.exe -Wait -PassThru `
  -ArgumentList @("/i", $installer, "/qn", "/norestart")
if ($msiProcess.ExitCode -ne 0) {
    throw "MySQL Installer 安装失败，msiexec 返回码：$($msiProcess.ExitCode)"
}
```

安装器完成后，运行官方控制台。下面的命令会从 MySQL 产品目录选择并安装 `MySQL Server`；安装期间按提示设置 `root` 密码、端口和 Windows 服务，不会在 PowerShell 历史记录中留下密码：

```powershell
$console = "${env:ProgramFiles(x86)}\MySQL\MySQL Installer for Windows\MySQLInstallerConsole.exe"
if (-not (Test-Path -LiteralPath $console)) {
    throw "未找到 MySQLInstallerConsole.exe：$console"
}

& $console --install server --setup-type=Server --auto-handle-prereqs
if ($LASTEXITCODE -ne 0) {
    throw "MySQL Server 安装或配置失败，返回码：$LASTEXITCODE"
}
```

完全无提示安装也可以使用 `--silent`，但必须在命令参数中传入 `root_passwd`，该密码可能出现在终端历史、进程列表或自动化日志中。因此本项目不提供可直接复制的含密码静默命令。确需自动化部署时，先运行以下命令查看当前安装器支持的配置字段，再通过受控的 CI/CD 密钥注入方式执行：

```powershell
& $console --configure --show-settings server
& $console help --action=install
```

#### 5.2.3 安装后验证和故障排查

安装完成后，以管理员身份打开 PowerShell，检查服务和端口：

```powershell
Get-Service -Name MySQL80
Test-NetConnection 127.0.0.1 -Port 3306
```

服务状态应为 `Running`，`TcpTestSucceeded` 应为 `True`。如果服务未启动：

```powershell
Start-Service -Name MySQL80
```

如果找不到 `MySQL80` 服务，列出实际安装的 MySQL 服务名：

```powershell
Get-Service -Name "*mysql*"
```

验证管理员登录。若 `mysql.exe` 没有加入 `PATH`，使用完整路径：

```powershell
& "C:\Program Files\MySQL\MySQL Server 8.0\bin\mysql.exe" -u root -p -e "SELECT VERSION();"
```

命令要求输入安装时设置的 `root` 密码。能输出版本号即表示本机 MySQL 可以工作。

#### 5.2.4 初始化本项目数据库并配置本地账号

本项目日常连接固定使用本机 MySQL 的 `newuser` 账号：

```text
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=farm
DB_USER=newuser
DB_PASS=123456
DB_ADMIN_USER=newuser
DB_ADMIN_PASS=123456
```

执行第 7 节的数据库初始化命令。该命令会导入 `mysql.sql`，其中包含 `DROP TABLE`，仅应在首次部署或已完成备份时执行。`newuser` 需要拥有创建数据库、删除和重建表的权限；若权限不足，仅将 `DB_ADMIN_USER` 和 `DB_ADMIN_PASS` 临时改为 `root` 账号，导入完成后再改回 `newuser`。

```powershell
.\deploy.ps1 -InitializeDatabase -Force `
  -MysqlExe "C:\Program Files\MySQL\MySQL Server 8.0\bin\mysql.exe"
```

初始化成功后，使用管理员账号确认本机 `newuser` 账号存在并授予 `farm` 库权限：

```powershell
& "C:\Program Files\MySQL\MySQL Server 8.0\bin\mysql.exe" -u root -p
```

在 MySQL 提示符中执行：

```sql
CREATE USER IF NOT EXISTS 'newuser'@'localhost'
  IDENTIFIED BY '123456';

GRANT SELECT, INSERT, UPDATE, DELETE, CREATE, ALTER, INDEX, REFERENCES, EXECUTE
  ON farm.* TO 'newuser'@'localhost';

FLUSH PRIVILEGES;
```

确认 `.env.local` 保持以下本机配置后，重新启动部署脚本：

```text
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=farm
DB_USER=newuser
DB_PASS=123456
DB_ADMIN_USER=newuser
DB_ADMIN_PASS=123456
```

```powershell
.\deploy.ps1
```

### 5.3 MySQL 命令行客户端

只有执行 `-InitializeDatabase` 导入 `mysql.sql` 时，才需要 `mysql.exe`。验证：

```powershell
mysql.exe --version
```

如果找不到，可以：

1. 把 MySQL 安装目录的 `bin` 加入系统 `PATH`；
2. 或在脚本中指定完整路径：

```powershell
.\deploy.ps1 `
  -InitializeDatabase `
  -Force `
  -MysqlExe "C:\Program Files\MySQL\MySQL Server 8.0\bin\mysql.exe"
```

脚本也会自动检查几个常见安装目录。

## 6. 第四步：创建 `.env.local`

`.env.local` 位于项目根目录，脚本会在启动前读取它。文件不存在时脚本使用本机默认值，但建议明确创建此文件，避免数据库账号配置不一致。

用记事本创建：

```powershell
notepad .env.local
```

填写以下内容。等号两边不要加空格：

```text
# Java 服务使用的数据库连接
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=farm
DB_USER=newuser
DB_PASS=123456

# 仅首次导入 mysql.sql 时使用；newuser 有建库权限时可与上面相同
DB_ADMIN_USER=newuser
DB_ADMIN_PASS=123456

# 开发板 TCP 地址；运行 deploy.ps1 时也会提示输入，并自动更新到 device 表
BOARD_IP=10.94.204.29
BOARD_PORT=8888
```

如果使用命令行参数，命令行参数优先于 `.env.local`，例如：

```powershell
.\deploy.ps1 -DatabaseHost 127.0.0.1 -DatabasePort 3306 -DatabaseUser newuser -DatabasePassword 123456 -BoardIp 10.94.204.29 -BoardPort 8888
```

数据库密码不会打印到脚本输出。脚本导入数据库时会在系统临时目录创建短期 MySQL 配置文件，导入结束后自动删除。

## 7. 第五步：首次初始化数据库

### 7.1 先备份已有数据库

如果 `farm` 库已经有重要数据，先使用管理员账号备份。示例：

```powershell
mysqldump.exe `
  --host=127.0.0.1 `
  --port=3306 `
  --user=数据库管理员 `
  --password `
  --databases farm `
  --result-file=.\farm-backup.sql
```

执行后按提示输入密码。备份成功后再考虑初始化。

### 7.2 导入完整数据库

第一次部署，使用：

```powershell
.\deploy.ps1 -InitializeDatabase -Force
```

如果不把数据库账号和开发板写入 `.env.local`，也可以全部通过参数传入：

```powershell
.\deploy.ps1 `
  -InitializeDatabase `
  -Force `
  -DatabaseHost 127.0.0.1 `
  -DatabasePort 3306 `
  -DatabaseName farm `
  -DatabaseUser newuser `
  -DatabasePassword "123456" `
  -DatabaseAdminUser "newuser" `
  -DatabaseAdminPassword "123456" `
  -BoardIp 10.94.204.29 `
  -BoardPort 8888
```

推荐把密码放在 `.env.local`，避免密码出现在 PowerShell 历史记录中。

如果不使用 `-Force`，脚本会显示数据覆盖警告，必须手动输入大写 `YES` 才会继续：

```powershell
.\deploy.ps1 -InitializeDatabase
```

### 7.3 脚本导入后做了什么

执行初始化时，脚本依次：

1. 检查 `mysql.sql` 是否存在；
2. 使用 `DB_ADMIN_USER` 和 `DB_ADMIN_PASS` 连接 MySQL；
3. 按原始 UTF-8 字节导入 `mysql.sql`；
4. 创建或恢复 `farm` 数据库及 14 张表；
5. 如果配置了 `BOARD_IP` 和 `BOARD_PORT`，执行：

```sql
UPDATE device
SET ip = '开发板IP', port = 开发板端口, online = 0
WHERE type <> '摄像头';
```

这一步是必要的，因为当前 `mysql.sql` 中保存的非摄像头设备地址是制作备份时的旧地址。脚本会把传感器和灌溉设备改成部署时传入的开发板地址，摄像头地址不会被修改。

6. 使用项目 JDK 和 Maven 编译 Java 后端；
7. 启动 Java 服务；
8. 检查首页和 `/api/plots` 数据库 API。

日常启动时如果填写了板子 IP 和端口，脚本也会执行同样的 `device` 表更新，不需要重新初始化数据库。

## 8. 第六步：编译并启动服务

### 8.1 日常部署命令

数据库已经初始化后，普通启动或更新代码执行：

```powershell
.\deploy.ps1
```

脚本会停止上一次由本脚本记录的 Java 服务，然后重新编译并启动。它不会再次导入 `mysql.sql`，不会删除数据库数据。

### 8.2 指定端口

默认服务端口为 `8080`。如果端口已占用，可以改成其他端口：

```powershell
.\deploy.ps1 -Port 9090
```

前端访问地址也要使用对应端口：

```text
http://localhost:9090/?simulator=0
```

### 8.3 允许其他电脑访问

服务会监听所有网卡。若需要局域网其他电脑访问，可以让脚本创建 Windows 防火墙入站规则。PowerShell 必须“以管理员身份运行”：

```powershell
.\deploy.ps1 -OpenFirewall
```

然后在其他电脑浏览器访问：

```text
http://部署机IP:8080/?simulator=0
```

如果不想自动创建防火墙规则，可以手动放行 TCP `8080`，或使用 `-Port` 指定的端口。

## 9. 第七步：验证部署

### 9.1 查看服务进程和端口

脚本成功后会输出 PID，也会把 PID 保存到：

```text
tmp\.smoke-server.pid
```

检查端口：

```powershell
Test-NetConnection localhost -Port 8080
```

`TcpTestSucceeded` 应为 `True`。

检查 PID：

```powershell
$serverPid = [int](Get-Content .\tmp\.smoke-server.pid)
Get-Process -Id $serverPid
```

### 9.2 检查首页

```powershell
$home = Invoke-WebRequest http://localhost:8080/ -UseBasicParsing
$home.StatusCode
```

应返回：

```text
200
```

### 9.3 检查数据库 API

```powershell
(Invoke-WebRequest http://localhost:8080/api/plots -UseBasicParsing).Content
(Invoke-WebRequest http://localhost:8080/api/devices -UseBasicParsing).Content
(Invoke-WebRequest http://localhost:8080/api/alarms -UseBasicParsing).Content
```

正常响应应包含：

```json
{"code":0,"data":...}
```

如果首页是 `200`，但 API 返回数据库错误，说明 Java 服务已启动，但数据库账号、表结构或数据库网络仍有问题。

### 9.4 检查登录

数据库初始化后，导出文件中的演示账号密码均为 `123456`：

| 账号 | 角色 |
|---|---|
| `farmer01` | 农户 |
| `admin01` | 农场管理员 |
| `sysadmin01` | 系统管理员 |
| `tianzun` | 农场管理员 |
| `admin02` | 农场管理员 |

接口测试：

```powershell
$body = @{
  username = "farmer01"
  password = "123456"
} | ConvertTo-Json

Invoke-RestMethod http://localhost:8080/api/auth/login `
  -Method Post `
  -ContentType "application/json; charset=utf-8" `
  -Body $body
```

返回 JSON 中的 `code` 应为 `0`。正式使用时应修改演示账号密码。

### 9.5 切换到真实数据库模式

前端为了答辩演示，默认开启模拟模式。部署完成后必须使用下面的地址打开真实数据模式：

```text
http://localhost:8080/?simulator=0
```

页面会把真实模式保存到浏览器 `localStorage`。如果之前打开过模拟模式，仍可在地址后加 `?simulator=0` 强制切换。

如果需要永久默认使用真实接口，可以编辑 `frontend\assets\js\api.js`：

```javascript
useMock: false
```

同源托管时不要修改 `baseUrl`，保持：

```javascript
baseUrl: ''
```

## 10. 开发板部署和数据采集

### 10.1 通信关系

开发板是 TCP 服务端，Java 服务是 TCP 客户端：

```text
开发板 10.94.204.29:8888
        ▲
        │ TCP 长连接
        │
Java 服务部署机
        │
        ▼
本机 MySQL 127.0.0.1:3306
```

Java 服务启动后会读取 `device` 表中非摄像头设备的 `ip` 和 `port`，连接同一 `ip:port` 的多个设备共享一条 TCP 长连接。

运行 `deploy.ps1` 时，脚本会提示输入板子 IP 和板子端口：

```text
请输入板子 IP（当前：10.94.204.29，直接回车保持不变）
请输入板子端口（当前：8888，直接回车保持不变）
```

输入后脚本会自动写入 `.env.local`，并把本机 MySQL 的 `device` 表中所有非摄像头设备更新到该 `ip:port`。随后 Java 服务启动，`BoardCollector` 会按这个地址自动建立 TCP 长连接。

### 10.2 开发板端口

开发板工程 `wifi-iot\app\sensor_tcp_server\sensor_server_demo.c` 当前使用：

```c
#define CONFIG_SERVER_PORT 8888
```

该程序支持：

```text
query  -> DATA TEMP:xx.xx HUMI:xx.xx LUX:xx.xx
ping   -> PONG
on     -> motor started
off    -> motor stopped
```

开发板每 5 秒向已连接客户端发送一次 `HEARTBEAT` 数据。

### 10.3 检查开发板网络

在 Java 服务部署机执行：

```powershell
Test-NetConnection 10.94.204.29 -Port 8888
```

必须显示 `TcpTestSucceeded : True`。

还要确认：

1. 开发板和部署机在可互通的网络中；
2. 开发板已经成功连接 Wi-Fi；
3. 开发板程序已经监听 `8888`；
4. 数据库 `device` 表中的 IP 和端口与实际开发板一致；正常运行 `deploy.ps1` 后这一步会自动完成；
5. 网络没有开启客户端隔离或禁止设备间 TCP 访问。

当前开发板固件只维护一个客户端连接。Java 服务运行时，不要再用另一个独立 TCP 客户端同时连接开发板，否则其中一个连接可能被开发板关闭。应通过网页的“刷新”功能、`/api/board/refresh` 接口和 `server.run.out.log` 判断采集是否正常。

### 10.4 验证数据库是否收到数据

服务运行一段时间后，通过 MySQL 查询：

```sql
USE farm;

SELECT id, name, type, ip, port, online
FROM device
WHERE type <> '摄像头'
ORDER BY id;

SELECT device_id, metric, value, collected_at
FROM sensor_data
ORDER BY collected_at DESC
LIMIT 20;
```

`device.online` 应为 `1`，`sensor_data` 应持续产生 `temp`、`humidity` 和 `lux` 三类记录。

### 10.5 更换开发板 IP 或端口

重新运行部署脚本，按提示输入新的板子 IP 和端口即可：

```powershell
.\deploy.ps1
```

也可以直接通过参数传入：

```powershell
.\deploy.ps1 -BoardIp 新开发板IP -BoardPort 新端口
```

脚本会把新的地址保存到 `.env.local`，并更新 `device` 表中所有非摄像头设备。已经有正式数据时，不要为了更换板子地址重新执行 `-InitializeDatabase`。

## 11. 可选功能配置

### 11.1 DeepSeek 智能问答

部署脚本会检查 `config\feishu.local.properties`、`.env.local` 和当前环境变量。没有配置 DeepSeek Key 时会提示：

```text
请输入 DeepSeek API Key（留空跳过）
```

填写后会写入本机私有配置文件 `config\feishu.local.properties`，该文件已被 `.gitignore` 忽略。也可以手动写入：

```text
SMART_QA_API_KEY=你的DeepSeek_API_Key
SMART_QA_API_URL=https://api.deepseek.com/chat/completions
SMART_QA_MODEL=deepseek-chat
```

服务读取 Key 的顺序为：

1. `SMART_QA_API_KEY`
2. `DEEPSEEK_API_KEY`
3. `API`
4. `config\feishu.local.properties`

修改后必须重启：

```powershell
.\deploy.ps1
```

没有 Key 不影响页面、登录、数据库 API、开发板采集和灌溉控制。

### 11.2 和风天气

部署脚本没有检测到和风天气 Key 时会提示：

```text
请输入和风天气 API Key（留空跳过）
```

填写后会写入 `config\feishu.local.properties`。也可以手动写入：

```text
QWEATHER_API_KEY=你的和风天气Key
QWEATHER_LOCATION=106.565952,29.642614
QWEATHER_HOST=https://ma5rk8cjh3.re.qweatherapi.com
```

没有 Key 时，天气接口会失败并由前端回退到本地模拟天气。

### 11.3 飞书机器人

部署脚本没有检测到飞书配置时会依次提示：

```text
请输入飞书 APPID（留空跳过）
请输入飞书 APPSECRET（留空跳过）
```

填写后会写入 `config\feishu.local.properties`。也可以复制模板后手动填写：

```powershell
Copy-Item config\feishu.local.properties.example config\feishu.local.properties
notepad config\feishu.local.properties
```

```properties
SMART_QA_API_KEY=你的模型服务Key
SMART_QA_API_URL=https://api.deepseek.com/chat/completions
SMART_QA_MODEL=deepseek-chat

QWEATHER_API_KEY=你的和风天气Key
QWEATHER_LOCATION=106.565952,29.642614
QWEATHER_HOST=https://ma5rk8cjh3.re.qweatherapi.com

FEISHU_ENABLED=true
FEISHU_APP_ID=你的飞书应用ID
FEISHU_APP_SECRET=你的飞书应用Secret
FEISHU_SMART_QA_ENABLED=true
FEISHU_REMEMBER_LAST_CHAT=true
```

飞书功能使用 SDK 长连接模式。部署机需要能够访问公网，不需要配置公网 HTTPS 回调地址。修改配置后重启 Java 服务。

### 11.4 YOLO 人体识别

人体识别不是基础服务启动的必需组件。项目默认需要：

```text
models\yolov8n.pt
workers\human_detection_worker.py
workers\requirements-detection.txt
tools\ffmpeg-9.0.1-essentials_build\bin\ffmpeg.exe
```

人体识别需要 Python。推荐使用 Python 3.11.x；`deploy.ps1 -StartDetectionWorker` 和 `workers\start_human_detection.ps1` 会自动检查 `.venv-detection`，缺少时会：

1. 查找本机 `python.exe`；
2. 未找到 Python 时，自动下载官方 Python `3.11.9` Windows 安装包到 `tools\python-3.11.9-amd64.exe`；
3. 静默安装 Python；
4. 创建 `.venv-detection` 虚拟环境；
5. 安装 `workers\requirements-detection.txt` 中的依赖。

也可以手动安装 Python。官方下载页：

```text
https://www.python.org/downloads/windows/
```

手动创建 Python 虚拟环境：

```powershell
python --version
python -m venv .venv-detection
.\.venv-detection\Scripts\python.exe -m pip install --upgrade pip
.\.venv-detection\Scripts\python.exe -m pip install -r workers\requirements-detection.txt
```

先启动 Java 服务，再启动 Worker：

```powershell
.\deploy.ps1
.\deploy.ps1 -StartDetectionWorker
```

如果只想配置和启动 Java 服务，不想在部署时填写 DeepSeek、和风天气、飞书配置，可以使用：

```powershell
.\deploy.ps1 -SkipOptionalConfigPrompt
```

也可以在一次部署中启动：

```powershell
.\deploy.ps1 -StartDetectionWorker
```

启动前必须存在 `.venv-detection\Scripts\python.exe` 和 `models\yolov8n.pt`。识别截图和视频保存在：

```text
data\detections
```

摄像头必须在“设备管理”中配置为类型 `摄像头`，并填写协议、IP、端口及必要的账号密码。

## 12. 停止、重启和日志

### 12.1 停止服务

```powershell
.\deploy.ps1 -Stop
```

脚本只停止 `tmp\.smoke-server.pid` 指向的服务，并兼容旧版根目录 `.smoke-server.pid`，不会通过端口号误杀其他程序。

### 12.2 查看日志

```powershell
Get-Content .\server.run.out.log -Wait
Get-Content .\server.run.err.log -Wait
```

可选人体识别 Worker 的日志：

```powershell
Get-Content .\worker.run.out.log -Wait
Get-Content .\worker.run.err.log -Wait
```

主要运行文件：

```text
server.run.out.log
server.run.err.log
worker.run.out.log
worker.run.err.log
tmp\.smoke-server.pid
target\
data\detections\
```

### 12.3 重启服务

```powershell
.\deploy.ps1
```

重启不会重新导入数据库。修改 `.env.local`、飞书配置、天气配置或模型配置后，都需要重启。

## 13. 部署脚本参数

查看参数说明：

```powershell
Get-Help .\deploy.ps1 -Detailed
```

常用参数：

| 参数 | 作用 |
|---|---|
| `-InitializeDatabase` | 导入根目录 `mysql.sql`，会覆盖项目表 |
| `-Force` | 跳过数据库覆盖确认，仅应与 `-InitializeDatabase` 一起使用 |
| `-BoardIp` | 开发板 IP；会保存到 `.env.local` 并写入非摄像头设备 |
| `-BoardPort` | 开发板端口；会保存到 `.env.local`，默认 `8888` |
| `-Port` | Java Web 服务端口，默认 `8080` |
| `-MysqlExe` | 指定 `mysql.exe` 完整路径 |
| `-MavenHome` | 指定 Maven 根目录 |
| `-JavaHome` | 指定 JDK 根目录；必须包含 `bin\java.exe` 和 `bin\javac.exe` |
| `-SkipJdkDownload` | 找不到 JDK 时禁止自动下载，直接报告错误 |
| `-NoPauseOnError` | 出错后不等待回车，适合自动化或批处理环境 |
| `-DatabaseHost` | 覆盖数据库地址 |
| `-DatabasePort` | 覆盖数据库端口 |
| `-DatabaseName` | 覆盖数据库名；初始化时必须是 `farm` |
| `-DatabaseUser` | 覆盖应用账号 |
| `-DatabasePassword` | 覆盖应用密码 |
| `-DatabaseAdminUser` | 覆盖数据库导入管理员账号 |
| `-DatabaseAdminPassword` | 覆盖数据库导入管理员密码 |
| `-StartDetectionWorker` | 启动 YOLO 人体识别 Worker |
| `-OpenFirewall` | 创建 TCP 入站防火墙规则 |
| `-SkipDatabaseCheck` | 跳过数据库网络和 API 检查，仅用于排查问题 |
| `-SkipOptionalConfigPrompt` | 跳过 DeepSeek、和风天气、飞书配置输入提示 |
| `-Stop` | 停止脚本记录的 Java 服务 |

## 14. 常见故障

### 14.1 找不到 `deploy.ps1`

原因是当前 PowerShell 不在项目根目录。执行：

```powershell
Set-Location D:\smoke
Get-ChildItem deploy.ps1
```

### 14.2 找不到项目 JDK

确认：

```powershell
Test-Path .\jdk-17.0.2\bin\java.exe
Test-Path .\jdk-17.0.2\bin\javac.exe
```

如果返回 `False`，把完整的 `jdk-17.0.2` 目录复制回项目，或使用：

```powershell
.\deploy.ps1 -JavaHome "C:\Program Files\Java\jdk-17"
```

### 14.3 找不到 Maven 或 Maven 下载失败

安装 Maven 3.9.x，确认：

```powershell
mvn.cmd -version
```

然后重新运行：

```powershell
.\deploy.ps1
```

如果部署机不能联网，把 Maven 解压到：

```text
项目根目录\tools\apache-maven-3.9.9
```

或者通过 `-MavenHome` 指定。

### 14.4 找不到 `mysql.exe`

只在初始化数据库时需要。使用：

```powershell
.\deploy.ps1 `
  -InitializeDatabase `
  -Force `
  -MysqlExe "C:\Program Files\MySQL\MySQL Server 8.0\bin\mysql.exe"
```

### 14.5 `Host 'xxx' is not allowed to connect`

这是 MySQL 用户主机授权问题，不是 Java 编译问题。让数据库管理员允许部署机 IP 登录，并检查：

```sql
SHOW GRANTS FOR '用户名'@'部署机IP';
```

授权后重新运行脚本。

### 14.6 数据库 TCP 可达，但 `/api/plots` 失败

依次检查：

1. `.env.local` 中的 `DB_HOST`、`DB_PORT`、`DB_NAME`、`DB_USER`、`DB_PASS`；
2. `DB_NAME` 是否为 `farm`；
3. `farm.plot` 表是否存在；
4. 应用账号是否有 `SELECT`、`INSERT`、`UPDATE`、`CREATE`、`ALTER` 权限；
5. `server.run.out.log` 和 `server.run.err.log`。

可以检查表：

```sql
USE farm;
SHOW TABLES;
SELECT COUNT(*) FROM plot;
SELECT COUNT(*) FROM device;
```

### 14.7 端口被占用

查看占用进程：

```powershell
Get-NetTCPConnection -LocalPort 8080 -State Listen |
  Select-Object LocalAddress, LocalPort, OwningProcess
```

如果不是本脚本启动的服务，不要直接停止它；改用其他端口：

```powershell
.\deploy.ps1 -Port 9090
```

### 14.8 首页能打开，但页面显示的是模拟数据

使用真实模式地址：

```text
http://localhost:8080/?simulator=0
```

如果浏览器曾保存过模拟模式，清除该站点的 `localStorage`，或再次访问带 `?simulator=0` 的地址。

### 14.9 开发板离线

先重新运行脚本，确认输入的板子 IP 和端口正确。脚本会把地址写入 `.env.local` 和本机 MySQL 的 `device` 表：

```powershell
.\deploy.ps1
```

检查：

```powershell
Test-NetConnection 开发板IP -Port 开发板端口
```

再检查 `device` 表：

```sql
SELECT id, name, type, ip, port, online
FROM farm.device
WHERE type <> '摄像头';
```

开发板离线不会阻止 Java 页面启动，但不会产生新的实时传感器数据。

### 14.10 人体识别 Worker 启动失败

确认：

```powershell
Test-Path .\.venv-detection\Scripts\python.exe
Test-Path .\models\yolov8n.pt
.\.venv-detection\Scripts\python.exe -m pip list | Select-String "ultralytics|opencv-python|requests"
```

如果 Python 环境缺失，直接让脚本自动准备并启动：

```powershell
.\deploy.ps1 -StartDetectionWorker
```

如果依赖缺失，手动重新安装：

```powershell
.\.venv-detection\Scripts\python.exe -m pip install --upgrade pip
.\.venv-detection\Scripts\python.exe -m pip install -r workers\requirements-detection.txt
```

## 15. 推荐的完整部署命令

以本机数据库 `127.0.0.1:3306`、应用账号 `newuser/123456`、开发板 `10.94.204.29:8888` 为例，推荐流程如下：

```powershell
Set-Location D:\smoke
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass

notepad .env.local

Get-Service -Name MySQL80
Test-NetConnection 127.0.0.1 -Port 3306
Test-NetConnection 10.94.204.29 -Port 8888

.\deploy.ps1 `
  -InitializeDatabase `
  -Force `
  -BoardIp 10.94.204.29 `
  -BoardPort 8888

Start-Process "http://localhost:8080/?simulator=0"
```

以后更新代码或重启服务，只执行：

```powershell
.\deploy.ps1
```

不要重复执行带 `-InitializeDatabase` 的命令，除非确认允许覆盖 `farm` 数据库中的项目表。
