# 智慧农业平台部署文档

本文档适用于本项目当前版本（Windows 部署）。部署前请先阅读“部署方式选择”，再决定手动部署或脚本部署。所有命令都在项目根目录执行，项目根目录就是同时包含 `pom.xml`、`mysql.sql`、`src` 和 `frontend` 的目录。

## 0. 部署方式选择（先读）

技术人员有两种部署方法，二选一即可：

| 方法 | 适用场景 | 需要手动做什么 | 脚本做什么 |
|---|---|---|---|
| 方法一：手动按步部署 | 需要完全掌控每一步、离线环境、脚本被安全策略限制、排查问题 | 自己安装 JDK、Maven、MySQL，自己改 `.env.local`，自己导入 `mysql.sql`，自己编译和启动 Java | 不使用 `deploy.ps1` |
| 方法二：运行部署脚本 | 推荐给普通部署和演示部署 | 自己准备 JDK 17 和 MySQL root 密码，按提示输入配置 | `deploy.ps1` 自动读取配置、检查环境、下载 Maven、启动本机 MySQL 服务、可选导入数据库、编译并启动服务 |

两种方法都要求当前电脑有完整 JDK 17 和本机 MySQL。项目 ZIP 包不包含 JDK；MySQL 使用当前电脑本机服务 `127.0.0.1:3306`，默认账号为 `root`。

首次部署必须导入 `mysql.sql`，否则网页能打开但数据库 API 可能失败。日常重启不要重复导入 `mysql.sql`，否则会覆盖 `farm` 数据库中的项目表。

### 0.1 方法一：手动按步部署

手动部署时不运行 `deploy.ps1`，按下面顺序自己执行。示例假设项目目录为 `D:\smoke`，JDK 安装目录为 `C:\Program Files\Eclipse Adoptium\jdk-17.0.20.1-hotspot`，MySQL 端口为 `3306`。

1. 解压项目 ZIP，并进入项目根目录：

```powershell
Expand-Archive -LiteralPath "$env:USERPROFILE\Downloads\smoke.zip" -DestinationPath D:\smoke -Force
Set-Location D:\smoke
Get-ChildItem pom.xml, mysql.sql, frontend\index.html
```

如果实际文件在 `D:\smoke\smoke-main\pom.xml`，则进入内层目录：

```powershell
Set-Location D:\smoke\smoke-main
```

2. 安装 JDK 17、Maven 3.9.x 和 MySQL 8。JDK 下载见第 4.1 节，Maven 下载见第 4.2 节，MySQL 下载见第 5.2 节。安装完成后检查：

```powershell
$jdk = "C:\Program Files\Eclipse Adoptium\jdk-17.0.20.1-hotspot"
$mysql = "C:\Program Files\MySQL\MySQL Server 8.0\bin\mysql.exe"
$mvn = "C:\apache-maven-3.9.16\bin\mvn.cmd"

Test-Path (Join-Path $jdk "bin\java.exe")
Test-Path (Join-Path $jdk "bin\javac.exe")
Test-Path $mysql
Test-Path $mvn
Test-NetConnection 127.0.0.1 -Port 3306
```

3. 创建或修改 `.env.local`：

```powershell
notepad .env.local
```

至少写入：

```text
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=farm
DB_USER=root
DB_PASS=你的root密码

DB_ADMIN_USER=root
DB_ADMIN_PASS=你的root密码

BOARD_IP=你的开发板IP
BOARD_PORT=8888
```

可选功能需要再补充 DeepSeek、和风天气和飞书配置，详见第 11 节。

4. 首次部署时手动导入数据库。此操作会按 `mysql.sql` 覆盖项目表：

```powershell
$mysql = "C:\Program Files\MySQL\MySQL Server 8.0\bin\mysql.exe"
$sql = (Resolve-Path .\mysql.sql).Path
cmd.exe /c "`"$mysql`" --host=127.0.0.1 --port=3306 --user=root --password --default-character-set=utf8mb4 < `"$sql`""
```

命令会提示输入 MySQL `root` 密码。

5. 如果使用开发板，手动把 `device` 表中的非摄像头设备改为当前开发板地址：

```powershell
$mysql = "C:\Program Files\MySQL\MySQL Server 8.0\bin\mysql.exe"
& $mysql --host=127.0.0.1 --port=3306 --user=root --password --database=farm `
  --execute="UPDATE device SET ip='你的开发板IP', port=8888, online=0 WHERE type <> '摄像头';"
```

6. 编译 Java 后端并生成运行时 classpath：

```powershell
$jdk = "C:\Program Files\Eclipse Adoptium\jdk-17.0.20.1-hotspot"
$mvn = "C:\apache-maven-3.9.9\bin\mvn.cmd"
$env:JAVA_HOME = $jdk
$env:Path = "$(Join-Path $jdk 'bin');$env:Path"

& $mvn "-DskipTests" "package" "-Dstyle.color=never"
& $mvn "dependency:build-classpath" "-Dmdep.outputFile=target\classpath.txt" "-Dmdep.outputAbsoluteArtifactFilename=true" "-DincludeScope=runtime" "-Dstyle.color=never"
```

7. 手动启动 Java 服务：

```powershell
$java = Join-Path $jdk "bin\java.exe"
$classpath = (Resolve-Path ".\target\classes").Path + ";" + (Get-Content ".\target\classpath.txt" -Raw).Trim()
& $java "-Djava.net.preferIPv4Stack=true" "-cp" $classpath "server.WebServer" "8080"
```

这个窗口会被 Java 服务占用，关闭窗口服务也会停止。部署成功后访问：

```text
http://localhost:8080/?simulator=0
```

### 0.2 方法二：运行部署脚本

脚本部署推荐用于普通部署。技术人员仍需先解压项目 ZIP、准备 JDK 17、安装本机 MySQL，然后运行 `deploy.ps1`。脚本会负责后续检查、导入、编译和启动。

第一次部署：

```powershell
Set-Location D:\smoke
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\deploy.ps1 -InitializeDatabase -Force
```

如果 JDK 安装在系统目录，而不是项目根目录 `jdk-17.0.2`：

```powershell
.\deploy.ps1 `
  -InitializeDatabase `
  -Force `
  -JavaHome "C:\Program Files\Eclipse Adoptium\jdk-17.0.20.1-hotspot"
```

日常启动：

```powershell
.\deploy.ps1
```

更详细的脚本部署流程见第 3.4 节。脚本参数说明见第 13 节。

### 0.3 外部软件和依赖的官方地址

部署过程中可能需要下载以下软件或依赖。下载时只使用下表中的项目官网、官网发布页或官网明确列出的构建页面：

| 软件或文件 | 用途 | 官方下载地址 |
|---|---|---|
| Eclipse Temurin JDK 17 | 编译和运行 Java 服务 | `https://adoptium.net/temurin/releases/?version=17&os=windows&arch=x64&package=jdk` |
| Apache Maven | 编译 Java 后端和下载 Java 依赖 | `https://maven.apache.org/download.cgi` |
| MySQL Community Server | 本机数据库 | `https://dev.mysql.com/downloads/installer/` |
| Python 3.11.9 | 人体识别 Worker | `https://www.python.org/downloads/windows/` |
| FFmpeg Windows 构建 | 生成人体识别回放视频 | `https://ffmpeg.org/download.html` |
| YOLOv8n 模型 | 人体识别模型 | `https://docs.ultralytics.com/models/yolov8/` |
| Java 依赖公共仓库 Maven Central | 下载 `pom.xml` 中的 Java 依赖 | `https://central.sonatype.com/` |
| Python 依赖公共仓库 PyPI | 下载 `requirements-detection.txt` 中的 Python 依赖 | `https://pypi.org/` |

其中，Maven、MySQL、Python、FFmpeg 和 YOLO 模型的详细下载方法分别见第 4.2、5.2 和 11.4 节。项目 ZIP 包、`.env.local` 和 API 密钥不是软件依赖下载项，不需要从第三方软件站下载。

## 1. 部署完成后的组成

项目由以下部分组成：

| 部分 | 目录或文件 | 是否为基础部署必需 |
|---|---|---|
| Java 服务端 | `src\server` | 必需 |
| 静态前端 | `frontend` | 必需 |
| 数据库完整导出 | `mysql.sql` | 首次初始化必需 |
| MySQL JDBC 驱动 | `lib\mysql-connector-j-8.0.33.jar` | 必需 |
| JDK 17 | 项目发布包不包含，需在部署机自行下载 | 必需 |
| Apache Maven | 项目发布包不包含，手动下载或由脚本下载 | 必需 |
| 开发板工程和固件 | `wifi-iot`、`Hi3861_wifiiot_app_allinone.bin` | 只有使用开发板时需要 |
| 人体识别 Worker | `workers`、`models`、`.venv-detection` | 可选 |
| 飞书机器人配置 | `config\feishu.local.properties` | 可选 |

项目不是 Spring Boot 项目，也不是 Node.js 项目。前端页面由 Java 服务直接托管，业务 API 由 Java 代码处理，数据库通过 MySQL JDBC 连接。

本项目提供两个 Java HTTP 入口：

- `server.WebServer`：基于 JDK `HttpServer`。
- `server.PlainWebServer`：基于阻塞式 `ServerSocket` 的回退入口。

`deploy.ps1` 和 `start-server-local.ps1` 默认使用项目正式入口 `server.WebServer`。该入口负责托管前端页面、初始化数据库表、加载 RAG、启动开发板采集和飞书机器人。

`server.PlainWebServer` 仅作为旧版本的回退入口保留，不再由部署脚本默认启动。若需要手工排查旧入口，可直接运行对应 Java 类，但它不会自动包含 `WebServer` 的全部启动流程。

## 2. 重要安全提示

### 2.1 不要把密码和密钥提交到 Git
以下文件可能包含数据库密码、模型 API Key 或飞书密钥：

```text
.env.local
config\feishu.local.properties
config\feishu.runtime.properties
```

这些文件只保存在部署机上，不要上传到代码仓库、聊天记录或公开网盘。

### 2.2 `mysql.sql` 会覆盖数据库表

根目录的 `mysql.sql` 是一个约 7 MB 的完整 MySQL 导出文件，包含：

- `CREATE DATABASE farm`
- `USE farm`
- 14 张表的结构
- 现有演示数据、历史传感器数据、告警数据和知识库数据
- 14 条 `DROP TABLE IF EXISTS`

因此，执行下面的命令会删除并重建 `farm` 库中的项目表，原有数据会被覆盖：

```powershell
.\deploy.ps1 -InitializeDatabase -Force -BoardIp 10.94.204.29 -BoardPort 8888
```

只有首次部署、明确需要恢复这份完整备份，或已完成数据库备份时，才执行 `-InitializeDatabase`。日常启动只执行：

```powershell
.\deploy.ps1
```

## 3. 第一步：准备项目文件

### 3.1 复制完整项目

把整个项目目录复制到部署机，例如：

```text
D:\smoke
```

不要只复制 `target` 或 `frontend`。至少要保留以下路径：

```text
D:\smoke\pom.xml
D:\smoke\mysql.sql
D:\smoke\src
D:\smoke\frontend
D:\smoke\lib\mysql-connector-j-8.0.33.jar
D:\smoke\deploy.ps1
```

项目发布包不包含 JDK。上面列出的路径是复制项目时必须保留的项目文件；JDK 请按照第 4.1 节单独下载。JDK 安装或解压完成后，必须能找到 `bin\java.exe` 和 `bin\javac.exe`。

项目路径可以不是 `D:\smoke`，但以后所有命令都必须在实际项目根目录执行。脚本会根据自身位置定位项目文件，不依赖固定盘符。

### 3.2 打开 PowerShell

按 `Win` 键搜索并打开 PowerShell，然后进入项目根目录：

```powershell
Set-Location D:\smoke
```

确认当前目录正确：

```powershell
Get-ChildItem pom.xml, mysql.sql, deploy.ps1, frontend\index.html
```

这四个文件都能列出时，说明目录正确。

### 3.3 允许当前窗口执行脚本

只对当前 PowerShell 窗口临时放宽执行策略，不修改系统永久策略：

```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
```

如果系统提示确认，输入 `Y`。

### 3.4 方法二：拿到项目 ZIP 包后运行脚本部署

拿到项目 ZIP 压缩包后，如果选择“方法二：运行部署脚本”，可以按下面顺序完整部署。示例假设 ZIP 文件名为 `smoke.zip`，目标目录为 `D:\smoke`。

#### 3.4.1 解压项目 ZIP

先新建目录并解压：

```powershell
New-Item -ItemType Directory -Force -Path D:\smoke | Out-Null
Expand-Archive -LiteralPath "$env:USERPROFILE\Downloads\smoke.zip" -DestinationPath D:\smoke -Force
Set-Location D:\smoke
```

如果 ZIP 解压后多了一层目录，例如实际文件在 `D:\smoke\smoke-main\pom.xml`，进入内层项目目录：

```powershell
Set-Location D:\smoke\smoke-main
```

确认当前目录就是项目根目录：

```powershell
Get-ChildItem pom.xml, mysql.sql, deploy.ps1, frontend\index.html
```

这四个文件都能列出时，才继续后面的步骤。

#### 3.4.2 允许运行脚本

在当前 PowerShell 窗口执行：

```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
```

如果系统提示确认，输入 `Y`。

#### 3.4.3 安装或准备 JDK 17

项目 ZIP 包不包含 JDK。请按第 4.1 节下载完整 Windows x64 JDK 17。推荐做法是下载 ZIP 版，解压后放在项目根目录并命名为：

```text
D:\smoke\jdk-17.0.2
```

如果使用 MSI 安装到系统目录，也可以不复制到项目中，但启动脚本时必须指定 `-JavaHome`：

```powershell
.\deploy.ps1 -JavaHome "C:\Program Files\Eclipse Adoptium\jdk-17.0.20.1-hotspot"
```

先检查 JDK 是否完整：

```powershell
Test-Path .\jdk-17.0.2\bin\java.exe
Test-Path .\jdk-17.0.2\bin\javac.exe
Test-Path .\jdk-17.0.2\lib\modules
```

如果没有放到项目目录，就检查实际安装目录：

```powershell
$jdk = "C:\Program Files\Eclipse Adoptium\jdk-17.0.20.1-hotspot"
Test-Path (Join-Path $jdk "bin\java.exe")
Test-Path (Join-Path $jdk "bin\javac.exe")
Test-Path (Join-Path $jdk "lib\modules")
```

三个结果都应为 `True`。

#### 3.4.4 安装并启动本机 MySQL

项目默认使用当前电脑本机 MySQL：

```text
127.0.0.1:3306
```

请先按第 5.2 节安装 MySQL 8，并记住安装时设置的 `root` 密码。安装后检查：

```powershell
Get-Service -Name MySQL80
Test-NetConnection 127.0.0.1 -Port 3306
```

如果服务存在但没启动：

```powershell
Start-Service -Name MySQL80
```

#### 3.4.5 修改本机配置 `.env.local`

在项目根目录创建或编辑 `.env.local`：

```powershell
notepad .env.local
```

填写本机数据库、开发板和可选 API 配置。至少建议写入：

```text
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=farm
DB_USER=root
# 可不写 DB_PASS；不写时 deploy.ps1 会提示输入 root 密码
# DB_PASS=你的root密码

DB_ADMIN_USER=root
# 可不写 DB_ADMIN_PASS；默认与 DB_PASS 相同
# DB_ADMIN_PASS=你的root密码

BOARD_IP=你的开发板IP
BOARD_PORT=8888
```

如果暂时没有 DeepSeek、和风天气或飞书机器人 Key，可以先不写。脚本运行时会提示输入；直接回车表示跳过，不影响基础网页、数据库和开发板采集启动。

如果是从别人电脑复制来的项目，重点检查 `.env.local` 中是否还保留旧配置，例如：

```text
DB_HOST=10.x.x.x
DB_USER=newuser
DB_PASS=123456
```

现在默认使用本机数据库，应改为 `DB_HOST=127.0.0.1` 和 `DB_USER=root`。

#### 3.4.6 首次导入数据库并启动程序

第一次在这台电脑部署时，执行：

```powershell
.\deploy.ps1 -InitializeDatabase -Force
```

如果 JDK 是 MSI 安装到系统目录，而不是放在项目根目录 `jdk-17.0.2`，执行：

```powershell
.\deploy.ps1 `
  -InitializeDatabase `
  -Force `
  -JavaHome "C:\Program Files\Eclipse Adoptium\jdk-17.0.20.1-hotspot"
```

脚本会依次完成：

1. 读取 `.env.local`；
2. 提示输入开发板 IP 和端口；
3. 提示输入 DeepSeek、和风天气、飞书配置；留空可跳过；
4. 检查 JDK、Maven、MySQL；
5. 导入 `mysql.sql` 到本机 MySQL 的 `farm` 库；
6. 把开发板 IP 和端口写入数据库 `device` 表；
7. 编译 Java 后端；
8. 启动 `server.WebServer`；
9. 检查首页和 `/api/plots`。

看到类似下面输出，表示基础部署成功：

```text
[OK] Java 后端构建完成。
[OK] 服务已监听：http://localhost:8080/
[OK] 数据库 API 检查通过。
```

#### 3.4.7 打开网页

部署成功后打开真实数据模式：

```powershell
Start-Process "http://localhost:8080/?simulator=0"
```

或者直接在浏览器输入：

```text
http://localhost:8080/?simulator=0
```

#### 3.4.8 以后日常启动

以后只需要在项目根目录执行：

```powershell
.\deploy.ps1
```

如果 JDK 没有放在项目目录，需要继续带上 `-JavaHome`：

```powershell
.\deploy.ps1 -JavaHome "C:\Program Files\Eclipse Adoptium\jdk-17.0.20.1-hotspot"
```

日常启动不要再加 `-InitializeDatabase`，否则会重新导入 `mysql.sql` 并覆盖项目表中的数据。

## 4. 第二步：准备 Java 和 Maven

### 4.1 下载并安装 JDK 17（项目不包含）

项目发布包不包含 Java 运行环境。部署前必须在当前电脑安装**完整 JDK 17**，不能只安装 JRE。项目代码按 Java 11 源码级别编译，但部署脚本需要使用 `javac.exe` 编译后端，因此必须同时存在：

```text
JDK目录\bin\java.exe
JDK目录\bin\javac.exe
JDK目录\lib\modules
```

#### 4.1.1 官方下载网址

推荐使用 Eclipse Temurin 17，它是免费的 OpenJDK 发行版。以下网址均为官方地址：

```text
官方下载选择页（推荐，页面可选择 Windows / x64 / JDK / 17）：
https://adoptium.net/temurin/releases/?version=17&os=windows&arch=x64&package=jdk

官方 API 最新稳定版 Windows x64 JDK ZIP 直链：
https://api.adoptium.net/v3/binary/latest/17/ga/windows/x64/jdk/hotspot/normal/eclipse?project=jdk

官方发布页：
https://github.com/adoptium/temurin17-binaries/releases

Oracle Java 17 官方下载页（备用，可能要求登录 Oracle 账号）：
https://www.oracle.com/java/technologies/javase/jdk17-archive-downloads.html
```

截至 `2026-09-02`，官方 API 返回的一个 Windows x64 JDK 17 版本为 `17.0.20.1`。如果需要固定版本，也可以使用以下官方 GitHub 直链：

```text
Windows x64 ZIP：
https://github.com/adoptium/temurin17-binaries/releases/download/jdk-17.0.20.1%2B1/OpenJDK17U-jdk_x64_windows_hotspot_17.0.20.1_1.zip

Windows x64 MSI：
https://github.com/adoptium/temurin17-binaries/releases/download/jdk-17.0.20.1%2B1/OpenJDK17U-jdk_x64_windows_hotspot_17.0.20.1_1.msi

ZIP 文件 SHA-256：
e53a79c3c3d86865bd7e787903884331068e71321714ffd44f145785affc7cb0
```

固定版本直链以后可能更新。遇到 404 时，打开官方发布页，选择最新的 `jdk-17...` 版本，并下载名称包含以下内容的文件：

```text
OpenJDK17U-jdk_x64_windows_hotspot_*.zip
```

不要下载 `jre`、`debugimage`、`sources` 或 `aarch64` 文件。普通 Intel/AMD Windows 电脑应选择 `x64`。

#### 4.1.2 方式一：下载 ZIP 并放入项目目录（推荐）

使用浏览器打开上面的官方选择页，选择：

1. `Version`：`17 - LTS`；
2. `Operating System`：`Windows`；
3. `Architecture`：`x64`；
4. `Package Type`：`JDK`；
5. 下载 ZIP 压缩包，不要下载 JRE。

也可以在 PowerShell 中下载官方 API ZIP 直链。下面的命令会把文件保存到当前用户的“下载”目录，并显示下载过程：

```powershell
$jdkUrl = "https://api.adoptium.net/v3/binary/latest/17/ga/windows/x64/jdk/hotspot/normal/eclipse?project=jdk"
$jdkZip = Join-Path $env:USERPROFILE "Downloads\OpenJDK17U-jdk-windows-x64.zip"

New-Item -ItemType Directory -Force -Path (Split-Path $jdkZip) | Out-Null
Invoke-WebRequest -Uri $jdkUrl -OutFile $jdkZip
Get-Item $jdkZip | Select-Object FullName, Length
```

校验文件（固定版本 ZIP 应得到上面列出的 SHA-256；最新版本以官方发布页的 `.sha256.txt` 文件为准）：

```powershell
Get-FileHash $jdkZip -Algorithm SHA256
```

解压时要注意目录层级。可以直接解压到项目根目录：

```powershell
Expand-Archive -LiteralPath $jdkZip -DestinationPath . -Force
```

如果解压出来的目录名称类似 `jdk-17.0.20.1-hotspot`，有两种用法。

用实际目录名启动：

```powershell
.\deploy.ps1 -JavaHome (Resolve-Path ".\jdk-17.0.20.1-hotspot").Path
```

或者把 JDK 目录改名为脚本默认查找的 `jdk-17.0.2`：

```powershell
Rename-Item ".\jdk-17.0.20.1-hotspot" ".\jdk-17.0.2"
.\deploy.ps1
```

最终目录必须是下面这种结构，不能多套一层目录：

```text
D:\smoke\jdk-17.0.2\bin\java.exe
D:\smoke\jdk-17.0.2\bin\javac.exe
D:\smoke\jdk-17.0.2\lib\modules
```

如果实际解压结果是下面这样，说明多套了一层目录，需要把内层 JDK 目录移出来：

```text
D:\smoke\jdk-17.0.20.1-hotspot\jdk-17.0.20.1-hotspot\bin\java.exe
```

#### 4.1.3 方式二：使用 MSI 安装

在官方选择页选择 Windows x64 的 MSI，或使用上面的 MSI 直链。双击安装包后：

1. 接受许可协议；
2. 保持默认安装组件；
3. 记下安装目录，例如：
   `C:\Program Files\Eclipse Adoptium\jdk-17.0.20.1-hotspot`；
4. 完成安装。

安装后先验证实际目录：

```powershell
$jdk = "C:\Program Files\Eclipse Adoptium\jdk-17.0.20.1-hotspot"
Test-Path (Join-Path $jdk "bin\java.exe")
Test-Path (Join-Path $jdk "bin\javac.exe")
Test-Path (Join-Path $jdk "lib\modules")
& (Join-Path $jdk "bin\java.exe") -version
& (Join-Path $jdk "bin\javac.exe") -version
```

三个 `Test-Path` 都应返回 `True`，版本号的主版本应为 `17`。然后从项目根目录执行：

```powershell
.\deploy.ps1 -JavaHome "C:\Program Files\Eclipse Adoptium\jdk-17.0.20.1-hotspot"
```

脚本默认只使用项目目录中的 `jdk-17.0.2`，不会自动使用其他电脑遗留的 `JAVA_HOME` 或 PATH 中的 Java，避免再次使用损坏或版本不兼容的 Java。项目目录没有 `jdk-17.0.2` 时，必须使用 `-JavaHome` 指定刚安装的 JDK 根目录，或者按 4.1.2 将目录放到项目根目录并改名。

#### 4.1.4 JDK 安装完成后的最终检查

在项目根目录执行：

```powershell
$jdk = "C:\Program Files\Eclipse Adoptium\jdk-17.0.20.1-hotspot"
& (Join-Path $jdk "bin\java.exe") -version
& (Join-Path $jdk "bin\javac.exe") -version
.\deploy.ps1 -JavaHome $jdk
```

如果出现 `Failed setting boot class path`、`Error occurred during initialization of VM` 或 Java 版本检查失败，通常是 `-JavaHome` 指到了 JRE、压缩包外层目录或不完整的 JDK。请确认 `-JavaHome` 的目录下直接存在 `bin\java.exe`、`bin\javac.exe` 和 `lib\modules`，不要指向 `bin` 目录本身。

### 4.2 Maven

项目发布包不包含 Maven。Maven 用于编译 Java 后端，并从 Maven Central 下载 `pom.xml` 中声明的 Java 依赖。手动部署必须先安装 Maven；脚本部署如果找不到 Maven，会自动下载项目指定的 Maven 3.9.9。

#### 4.2.1 Maven 官方下载网址

以下地址是 Apache Maven 官方地址：

```text
Apache Maven 官方下载页（推荐，版本会更新）：
https://maven.apache.org/download.cgi

Apache Maven 官方安装说明：
https://maven.apache.org/install.html

当前官方稳定版 Windows ZIP（截至 2026-09-02 为 3.9.16）：
https://dlcdn.apache.org/maven/maven-3/3.9.16/binaries/apache-maven-3.9.16-bin.zip

当前官方稳定版 ZIP SHA-512：
https://downloads.apache.org/maven/maven-3/3.9.16/binaries/apache-maven-3.9.16-bin.zip.sha512

项目脚本固定使用的 Maven 3.9.9 ZIP：
https://archive.apache.org/dist/maven/maven-3/3.9.9/binaries/apache-maven-3.9.9-bin.zip

项目脚本固定版本的 ZIP SHA-512：
https://archive.apache.org/dist/maven/maven-3/3.9.9/binaries/apache-maven-3.9.9-bin.zip.sha512
```

不要下载 `src` 源码包，也不要下载 Maven 4 的预览版本。Windows 普通电脑下载文件名中的 `bin.zip`。

#### 4.2.2 手动下载和安装 Maven

手动部署时，推荐下载官方 Maven 3.9.16 ZIP。下载后解压到不含中文和空格的目录，例如：

```text
C:\apache-maven-3.9.16
```

PowerShell 下载和校验示例：

```powershell
$mavenUrl = "https://dlcdn.apache.org/maven/maven-3/3.9.16/binaries/apache-maven-3.9.16-bin.zip"
$mavenShaUrl = "https://downloads.apache.org/maven/maven-3/3.9.16/binaries/apache-maven-3.9.16-bin.zip.sha512"
$mavenZip = Join-Path $env:USERPROFILE "Downloads\apache-maven-3.9.16-bin.zip"

New-Item -ItemType Directory -Force -Path (Split-Path $mavenZip) | Out-Null
Invoke-WebRequest -Uri $mavenUrl -OutFile $mavenZip
$expectedSha512 = ((Invoke-WebRequest -Uri $mavenShaUrl -UseBasicParsing).Content -split "\s+")[0].ToLower()
$actualSha512 = (Get-FileHash -LiteralPath $mavenZip -Algorithm SHA512).Hash.ToLower()
if ($actualSha512 -ne $expectedSha512) {
    throw "Maven ZIP SHA-512 校验失败。"
}

Expand-Archive -LiteralPath $mavenZip -DestinationPath "C:\" -Force
```

解压后确认目录中直接存在 `bin\mvn.cmd`，不要多套一层目录：

```powershell
Test-Path "C:\apache-maven-3.9.16\bin\mvn.cmd"
& "C:\apache-maven-3.9.16\bin\mvn.cmd" -version
```

输出中应能看到 Maven 版本和 Java 17 运行环境。

手动部署的第 0.1 节示例使用：

```powershell
$mvn = "C:\apache-maven-3.9.16\bin\mvn.cmd"
```

如果 Maven 放在其他目录，把 `$mvn` 改成实际的 `bin\mvn.cmd` 完整路径即可。

#### 4.2.3 脚本自动下载 Maven

运行 `deploy.ps1` 时，脚本按以下顺序寻找 Maven：

1. 当前 `PATH` 中的 `mvn.cmd`；
2. 项目目录 `tools\apache-maven-3.9.9\bin\mvn.cmd`；
3. 如果仍未找到，从 Apache 官方归档地址下载 Maven 3.9.9 到 `tools` 目录，并显示下载进度；
4. 自动解压到 `tools\apache-maven-3.9.9`。

脚本使用的下载地址是：

```text
https://archive.apache.org/dist/maven/maven-3/3.9.9/binaries/apache-maven-3.9.9-bin.zip
```

运行脚本的电脑需要能够访问该地址以及 Maven Central。若无法联网，请在可以联网的电脑下载并解压 Maven，再把完整目录复制到：

```text
项目根目录\tools\apache-maven-3.9.9
```

或者使用参数指定已安装的 Maven：

```powershell
.\deploy.ps1 -MavenHome "C:\apache-maven-3.9.16"
```

#### 4.2.4 Java 依赖下载地址

首次构建时，Maven 还会从 Maven Central 下载项目依赖。依赖版本由 `pom.xml` 管理：

```text
Maven Central 官方主页：
https://central.sonatype.com/

飞书 SDK：
https://central.sonatype.com/artifact/com.larksuite.oapi/oapi-sdk/2.4.19

MySQL JDBC 驱动：
https://central.sonatype.com/artifact/com.mysql/mysql-connector-j/8.0.33
```

如果部署机不能访问公网，需要提前准备 Maven 本地仓库，并确认其中包含上述依赖及其传递依赖。只复制 `mvn.cmd` 不能替代 Maven 依赖仓库。

## 5. 第三步：准备 MySQL

### 5.1 MySQL 服务要求

建议使用 MySQL 8.x，要求：

- MySQL 服务已经启动。
- 默认端口为 `3306`，若使用其他端口，要在 `.env.local` 中修改。
- Java 服务连接本机地址 `127.0.0.1`。
- 默认使用当前电脑 MySQL 的 `root` 账号；密码为安装 MySQL 时设置的 root 密码。
- 字符集支持 `utf8mb4`。

`deploy.ps1` 检测到本机 MySQL/MariaDB 服务处于停止状态时，会自动尝试启动服务，并等待 `127.0.0.1:3306` 可用。

### 5.2 在本机安装 MySQL 8

如果 Java 服务和数据库部署在同一台 Windows 电脑上，推荐安装 MySQL Community Server 8.x。本节以默认安装目录 `C:\Program Files\MySQL\MySQL Server 8.0`、默认服务名 `MySQL80`、默认端口 `3306` 为例。

截至 2026 年 9 月 2 日，MySQL Installer 的当前 8.0 版本为 `8.0.46`。优先使用下面的官方下载页；页面会随版本更新，文档中的直链仅用于当前版本。

当 `.env.local` 中的 `DB_HOST` 是 `localhost`、`127.0.0.1`、`::1` 或本机计算机名，且脚本无法连接数据库端口时，`deploy.ps1` 会先检查本机是否存在 MySQL/MariaDB Windows 服务。未发现服务时，脚本会自动将 MySQL Installer 下载到 `tools\mysql-installer-web-community-8.0.46.0.msi`，校验 MD5 后以管理员权限打开安装向导。

自动流程不会替用户设置 `root` 密码，也不会自动导入项目数据库；完成向导后仍需按照 5.2.4 初始化 `farm` 库。若只想让脚本报告数据库错误而不下载，使用：

```powershell
.\deploy.ps1 -SkipMySqlInstallerDownload
```

```text
官方选择下载页：
https://dev.mysql.com/downloads/installer/

联网安装器（约 2 MB，安装过程中下载 MySQL Server）：
https://cdn.mysql.com/Downloads/MySQLInstaller/mysql-installer-web-community-8.0.46.0.msi

离线完整安装器（约 566 MB，适合目标机安装时不能联网）：
https://cdn.mysql.com/Downloads/MySQLInstaller/mysql-installer-community-8.0.46.0.msi
```

打开官方下载页后：

1. 在 `MySQL Installer` 页面选择 Windows 的 MSI Installer。
2. 部署机安装时可以联网，下载 `mysql-installer-web-community-8.0.46.0.msi`。
3. 部署机不能联网，下载 `mysql-installer-community-8.0.46.0.msi` 并拷贝到部署机。
4. 如果网页要求登录 Oracle 账号，点击页面下方的 `No thanks, just start my download.` 开始下载。

下载完成后可以校验文件完整性。官方页面当前列出的 MD5 分别为：

```powershell
# 联网安装器的预期 MD5：210420aef5b5006ab54bb1dab4183768
# 离线完整安装器的预期 MD5：270575e788f414c29a42e41203161727
Get-FileHash .\mysql-installer-web-community-8.0.46.0.msi -Algorithm MD5
```

#### 5.2.1 推荐：图形安装

双击下载的 MSI，以管理员身份运行，按以下选项配置：

1. `Setup Type` 选择 `Server only`。
2. 出现依赖项页面时点击 `Execute`，等待所有依赖安装完成。
3. `Type and Networking` 选择 `Development Computer`。
4. 勾选 `TCP/IP`，端口保持 `3306`。只供本机 Java 服务使用时，不勾选 `Open Windows Firewall ports for network access`。
5. `Authentication Method` 保持默认的 `Use Strong Password Encryption`。
6. 为 `root` 设置高强度密码；不要留空，也不要使用示例密码。
7. `Windows Service` 保持服务名 `MySQL80`，勾选 `Start the MySQL Server at System Startup` 和 `Standard System Account`。
8. 在 `Apply Configuration` 页面点击 `Execute`，所有步骤显示成功后点击 `Finish`。

如果 `3306` 已被占用，先查找占用者：

```powershell
Get-NetTCPConnection -LocalPort 3306 -State Listen -ErrorAction SilentlyContinue |
  Select-Object LocalAddress, LocalPort, OwningProcess
```

可以停止冲突服务，或改用未占用端口；改端口后必须在 `.env.local` 中同步设置 `DB_PORT`。

#### 5.2.2 命令行下载和安装

可以通过命令行安装。MySQL 官方的 `MySQLInstallerConsole.exe` 支持安装、配置和 `--silent` 无提示模式。对于本项目，推荐使用“命令行下载 + 控制台交互配置”：不需要把 `root` 密码写进命令历史或进程参数。

以管理员身份启动 PowerShell，并确认当前窗口已经提升权限：

```powershell
$currentUser = [Security.Principal.WindowsIdentity]::GetCurrent()
$principal = New-Object Security.Principal.WindowsPrincipal($currentUser)
if (-not $principal.IsInRole([Security.Principal.WindowsBuiltInRole]::Administrator)) {
    throw "请以管理员身份重新打开 PowerShell 后再执行安装。"
}
```

联网下载安装器并静默安装 MySQL Installer 本身：

```powershell
$version = "8.0.46.0"
$installer = Join-Path $env:TEMP "mysql-installer-web-community-$version.msi"
$url = "https://cdn.mysql.com/Downloads/MySQLInstaller/mysql-installer-web-community-$version.msi"

Invoke-WebRequest -Uri $url -OutFile $installer
if ((Get-FileHash $installer -Algorithm MD5).Hash -ne "210420AEF5B5006AB54BB1DAB4183768") {
    throw "MySQL Installer 文件校验失败，请删除下载文件后重新下载。"
}

$msiProcess = Start-Process msiexec.exe -Wait -PassThru `
  -ArgumentList @("/i", $installer, "/qn", "/norestart")
if ($msiProcess.ExitCode -ne 0) {
    throw "MySQL Installer 安装失败，msiexec 返回码：$($msiProcess.ExitCode)"
}
```

安装器完成后，运行官方控制台。下面的命令会从 MySQL 产品目录选择并安装 `MySQL Server`；安装期间按提示设置 `root` 密码、端口和 Windows 服务，不会在 PowerShell 历史记录中留下密码：

```powershell
$console = "${env:ProgramFiles(x86)}\MySQL\MySQL Installer for Windows\MySQLInstallerConsole.exe"
if (-not (Test-Path -LiteralPath $console)) {
    throw "未找到 MySQLInstallerConsole.exe：$console"
}

& $console --install server --setup-type=Server --auto-handle-prereqs
if ($LASTEXITCODE -ne 0) {
    throw "MySQL Server 安装或配置失败，返回码：$LASTEXITCODE"
}
```

完全无提示安装也可以使用 `--silent`，但必须在命令参数中传入 `root_passwd`，该密码可能出现在终端历史、进程列表或自动化日志中。因此本项目不提供可直接复制的含密码静默命令。确需自动化部署时，先运行以下命令查看当前安装器支持的配置字段，再通过受控的 CI/CD 密钥注入方式执行：

```powershell
& $console --configure --show-settings server
& $console help --action=install
```

#### 5.2.3 安装后验证和故障排查

安装完成后，以管理员身份打开 PowerShell，检查服务和端口：

```powershell
Get-Service -Name MySQL80
Test-NetConnection 127.0.0.1 -Port 3306
```

服务状态应为 `Running`，`TcpTestSucceeded` 应为 `True`。如果服务未启动：

```powershell
Start-Service -Name MySQL80
```

如果找不到 `MySQL80` 服务，列出实际安装的 MySQL 服务名：

```powershell
Get-Service -Name "*mysql*"
```

验证管理员登录。若 `mysql.exe` 没有加入 `PATH`，使用完整路径：

```powershell
& "C:\Program Files\MySQL\MySQL Server 8.0\bin\mysql.exe" -u root -p -e "SELECT VERSION();"
```

命令要求输入安装时设置的 `root` 密码。能输出版本号即表示本机 MySQL 可以工作。

#### 5.2.4 初始化本项目数据库

本项目默认直接使用当前电脑 MySQL 的 `root` 账号连接本机数据库。root 密码是安装 MySQL 时自己设置的密码，脚本不会固定为 `newuser/123456`。

```text
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=farm
DB_USER=root
# 可不写 DB_PASS；不写时 deploy.ps1 会提示输入 root 密码
# DB_PASS=你的root密码
DB_ADMIN_USER=root
# 可不写 DB_ADMIN_PASS；默认与 DB_PASS 相同
# DB_ADMIN_PASS=你的root密码
```

执行第 7 节的数据库初始化命令。该命令会导入 `mysql.sql`，其中包含 `DROP TABLE`，仅应在首次部署或已完成备份时执行。使用 root 登录时，不需要再创建额外的应用数据库账号。

```powershell
.\deploy.ps1 -InitializeDatabase -Force `
  -MysqlExe "C:\Program Files\MySQL\MySQL Server 8.0\bin\mysql.exe"
```

如果没有在 `.env.local` 中填写 `DB_PASS`，脚本会提示输入：

```text
请输入本机 MySQL root 密码（留空表示无密码）
```

确认 `.env.local` 保持以下本机配置后，重新启动部署脚本：

```text
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=farm
DB_USER=root
# 可不写 DB_PASS；不写时 deploy.ps1 会提示输入 root 密码
# DB_PASS=你的root密码
DB_ADMIN_USER=root
# 可不写 DB_ADMIN_PASS；默认与 DB_PASS 相同
# DB_ADMIN_PASS=你的root密码
```

```powershell
.\deploy.ps1
```

### 5.3 MySQL 命令行客户端

只有执行 `-InitializeDatabase` 导入 `mysql.sql` 时，才需要 `mysql.exe`。验证：

```powershell
mysql.exe --version
```

如果找不到，可以：

1. 把 MySQL 安装目录的 `bin` 加入系统 `PATH`；
2. 或在脚本中指定完整路径：

```powershell
.\deploy.ps1 `
  -InitializeDatabase `
  -Force `
  -MysqlExe "C:\Program Files\MySQL\MySQL Server 8.0\bin\mysql.exe"
```

脚本也会自动检查几个常见安装目录。

## 6. 第四步：创建 `.env.local`

`.env.local` 位于项目根目录，脚本会在启动前读取它。文件不存在时脚本使用本机默认值，但建议明确创建此文件，避免数据库账号配置不一致。

如果是从旧电脑复制过来的项目，先检查 `.env.local`。旧版本可能写着 `DB_USER=newuser` 和 `DB_PASS=123456`；脚本会优先使用文件里的配置。要使用当前电脑 MySQL 的 root 账号，请删除这两行或改成 root 配置。

用记事本创建：

```powershell
notepad .env.local
```

填写以下内容。等号两边不要加空格：

```text
# Java 服务使用的数据库连接
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=farm
DB_USER=root
# 可不写 DB_PASS；不写时 deploy.ps1 会提示输入 root 密码
# DB_PASS=你的root密码

# 仅首次导入 mysql.sql 时使用；默认与上面的 root 账号相同
DB_ADMIN_USER=root
# 可不写 DB_ADMIN_PASS；默认与 DB_PASS 相同
# DB_ADMIN_PASS=你的root密码

# 开发板 TCP 地址；运行 deploy.ps1 时也会提示输入，并自动更新到 device 表
BOARD_IP=10.94.204.29
BOARD_PORT=8888
```

如果使用命令行参数，命令行参数优先于 `.env.local`，例如：

```powershell
.\deploy.ps1 -DatabaseHost 127.0.0.1 -DatabasePort 3306 -DatabaseUser root -DatabasePassword "你的root密码" -BoardIp 10.94.204.29 -BoardPort 8888
```

数据库密码不会打印到脚本输出。脚本导入数据库时会在系统临时目录创建短期 MySQL 配置文件，导入结束后自动删除。

## 7. 第五步：首次初始化数据库

### 7.1 先备份已有数据库

如果 `farm` 库已经有重要数据，先使用管理员账号备份。示例：

```powershell
mysqldump.exe `
  --host=127.0.0.1 `
  --port=3306 `
  --user=数据库管理员 `
  --password `
  --databases farm `
  --result-file=.\farm-backup.sql
```

执行后按提示输入密码。备份成功后再考虑初始化。

### 7.2 导入完整数据库

第一次部署，使用：

```powershell
.\deploy.ps1 -InitializeDatabase -Force
```

如果不把数据库账号和开发板写入 `.env.local`，也可以全部通过参数传入：

```powershell
.\deploy.ps1 `
  -InitializeDatabase `
  -Force `
  -DatabaseHost 127.0.0.1 `
  -DatabasePort 3306 `
  -DatabaseName farm `
  -DatabaseUser root `
  -DatabasePassword "你的root密码" `
  -DatabaseAdminUser root `
  -DatabaseAdminPassword "你的root密码" `
  -BoardIp 10.94.204.29 `
  -BoardPort 8888
```

推荐把密码放在 `.env.local`，避免密码出现在 PowerShell 历史记录中。

如果不使用 `-Force`，脚本会显示数据覆盖警告，必须手动输入大写 `YES` 才会继续：

```powershell
.\deploy.ps1 -InitializeDatabase
```

### 7.3 脚本导入后做了什么

执行初始化时，脚本依次：

1. 检查 `mysql.sql` 是否存在；
2. 使用 `DB_ADMIN_USER` 和 `DB_ADMIN_PASS` 连接 MySQL；
3. 按原始 UTF-8 字节导入 `mysql.sql`；
4. 创建或恢复 `farm` 数据库及 14 张表；
5. 如果配置了 `BOARD_IP` 和 `BOARD_PORT`，执行：

```sql
UPDATE device
SET ip = '开发板IP', port = 开发板端口, online = 0
WHERE type <> '摄像头';
```

这一步是必要的，因为当前 `mysql.sql` 中保存的非摄像头设备地址是制作备份时的旧地址。脚本会把传感器和灌溉设备改成部署时传入的开发板地址，摄像头地址不会被修改。

6. 使用部署机已安装的 JDK 17 和 Maven 编译 Java 后端；
7. 启动 Java 服务；
8. 检查首页和 `/api/plots` 数据库 API。

日常启动时如果填写了板子 IP 和端口，脚本也会执行同样的 `device` 表更新，不需要重新初始化数据库。

## 8. 第六步：编译并启动服务

### 8.1 日常部署命令

数据库已经初始化后，普通启动或更新代码执行：

```powershell
.\deploy.ps1
```

脚本会停止上一次由本脚本记录的 Java 服务，然后重新编译并启动。它不会再次导入 `mysql.sql`，不会删除数据库数据。

### 8.2 指定端口

默认服务端口为 `8080`。如果端口已占用，可以改成其他端口：

```powershell
.\deploy.ps1 -Port 9090
```

前端访问地址也要使用对应端口：

```text
http://localhost:9090/?simulator=0
```

### 8.3 允许其他电脑访问

服务会监听所有网卡。若需要局域网其他电脑访问，可以让脚本创建 Windows 防火墙入站规则。PowerShell 必须“以管理员身份运行”：

```powershell
.\deploy.ps1 -OpenFirewall
```

然后在其他电脑浏览器访问：

```text
http://部署机IP:8080/?simulator=0
```

如果不想自动创建防火墙规则，可以手动放行 TCP `8080`，或使用 `-Port` 指定的端口。

## 9. 第七步：验证部署

### 9.1 查看服务进程和端口

脚本成功后会输出 PID，也会把 PID 保存到：

```text
tmp\.smoke-server.pid
```

检查端口：

```powershell
Test-NetConnection localhost -Port 8080
```

`TcpTestSucceeded` 应为 `True`。

检查 PID：

```powershell
$serverPid = [int](Get-Content .\tmp\.smoke-server.pid)
Get-Process -Id $serverPid
```

### 9.2 检查首页

```powershell
$home = Invoke-WebRequest http://localhost:8080/ -UseBasicParsing
$home.StatusCode
```

应返回：

```text
200
```

### 9.3 检查数据库 API

```powershell
(Invoke-WebRequest http://localhost:8080/api/plots -UseBasicParsing).Content
(Invoke-WebRequest http://localhost:8080/api/devices -UseBasicParsing).Content
(Invoke-WebRequest http://localhost:8080/api/alarms -UseBasicParsing).Content
```

正常响应应包含：

```json
{"code":0,"data":...}
```

如果首页是 `200`，但 API 返回数据库错误，说明 Java 服务已启动，但数据库账号、表结构或数据库网络仍有问题。

### 9.4 检查登录

数据库初始化后，导出文件中的演示账号密码均为 `123456`：

| 账号 | 角色 |
|---|---|
| `farmer01` | 农户 |
| `admin01` | 农场管理员 |
| `sysadmin01` | 系统管理员 |
| `tianzun` | 农场管理员 |
| `admin02` | 农场管理员 |

接口测试：

```powershell
$body = @{
  username = "farmer01"
  password = "123456"
} | ConvertTo-Json

Invoke-RestMethod http://localhost:8080/api/auth/login `
  -Method Post `
  -ContentType "application/json; charset=utf-8" `
  -Body $body
```

返回 JSON 中的 `code` 应为 `0`。正式使用时应修改演示账号密码。

### 9.5 切换到真实数据库模式

前端为了答辩演示，默认开启模拟模式。部署完成后必须使用下面的地址打开真实数据模式：

```text
http://localhost:8080/?simulator=0
```

页面会把真实模式保存到浏览器 `localStorage`。如果之前打开过模拟模式，仍可在地址后加 `?simulator=0` 强制切换。

如果需要永久默认使用真实接口，可以编辑 `frontend\assets\js\api.js`：

```javascript
useMock: false
```

同源托管时不要修改 `baseUrl`，保持：

```javascript
baseUrl: ''
```

## 10. 开发板部署和数据采集

### 10.1 通信关系

开发板是 TCP 服务端，Java 服务是 TCP 客户端：

```text
开发板 10.94.204.29:8888
        ▲
        │ TCP 长连接
        │
Java 服务部署机
        │
        ▼
本机 MySQL 127.0.0.1:3306
```

Java 服务启动后会读取 `device` 表中非摄像头设备的 `ip` 和 `port`，连接同一 `ip:port` 的多个设备共享一条 TCP 长连接。

运行 `deploy.ps1` 时，脚本会提示输入板子 IP 和板子端口：

```text
请输入板子 IP（当前：10.94.204.29，直接回车保持不变）
请输入板子端口（当前：8888，直接回车保持不变）
```

输入后脚本会自动写入 `.env.local`，并把本机 MySQL 的 `device` 表中所有非摄像头设备更新到该 `ip:port`。随后 Java 服务启动，`BoardCollector` 会按这个地址自动建立 TCP 长连接。

### 10.2 开发板端口

开发板工程 `wifi-iot\app\sensor_tcp_server\sensor_server_demo.c` 当前使用：

```c
#define CONFIG_SERVER_PORT 8888
```

该程序支持：

```text
query  -> DATA TEMP:xx.xx HUMI:xx.xx LUX:xx.xx
ping   -> PONG
on     -> motor started
off    -> motor stopped
```

开发板每 5 秒向已连接客户端发送一次 `HEARTBEAT` 数据。

### 10.3 检查开发板网络

在 Java 服务部署机执行：

```powershell
Test-NetConnection 10.94.204.29 -Port 8888
```

必须显示 `TcpTestSucceeded : True`。

还要确认：

1. 开发板和部署机在可互通的网络中；
2. 开发板已经成功连接 Wi-Fi；
3. 开发板程序已经监听 `8888`；
4. 数据库 `device` 表中的 IP 和端口与实际开发板一致；正常运行 `deploy.ps1` 后这一步会自动完成；
5. 网络没有开启客户端隔离或禁止设备间 TCP 访问。

当前开发板固件只维护一个客户端连接。Java 服务运行时，不要再用另一个独立 TCP 客户端同时连接开发板，否则其中一个连接可能被开发板关闭。应通过网页的“刷新”功能、`/api/board/refresh` 接口和 `server.run.out.log` 判断采集是否正常。

### 10.4 验证数据库是否收到数据

服务运行一段时间后，通过 MySQL 查询：

```sql
USE farm;

SELECT id, name, type, ip, port, online
FROM device
WHERE type <> '摄像头'
ORDER BY id;

SELECT device_id, metric, value, collected_at
FROM sensor_data
ORDER BY collected_at DESC
LIMIT 20;
```

`device.online` 应为 `1`，`sensor_data` 应持续产生 `temp`、`humidity` 和 `lux` 三类记录。

### 10.5 更换开发板 IP 或端口

重新运行部署脚本，按提示输入新的板子 IP 和端口即可：

```powershell
.\deploy.ps1
```

也可以直接通过参数传入：

```powershell
.\deploy.ps1 -BoardIp 新开发板IP -BoardPort 新端口
```

脚本会把新的地址保存到 `.env.local`，并更新 `device` 表中所有非摄像头设备。已经有正式数据时，不要为了更换板子地址重新执行 `-InitializeDatabase`。

## 11. 可选功能配置

### 11.1 DeepSeek 智能问答

部署脚本会检查 `config\feishu.local.properties`、`.env.local` 和当前环境变量。没有配置 DeepSeek Key 时会提示：

```text
请输入 DeepSeek API Key（留空跳过）
```

填写后会写入本机私有配置文件 `config\feishu.local.properties`，该文件已被 `.gitignore` 忽略。也可以手动写入：

```text
SMART_QA_API_KEY=你的DeepSeek_API_Key
SMART_QA_API_URL=https://api.deepseek.com/chat/completions
SMART_QA_MODEL=deepseek-chat
```

服务读取 Key 的顺序为：

1. `SMART_QA_API_KEY`
2. `DEEPSEEK_API_KEY`
3. `API`
4. `config\feishu.local.properties`

修改后必须重启：

```powershell
.\deploy.ps1
```

没有 Key 不影响页面、登录、数据库 API、开发板采集和灌溉控制。

### 11.2 和风天气

部署脚本没有检测到和风天气 Key 时会提示：

```text
请输入和风天气 API Key（留空跳过）
```

填写后会写入 `config\feishu.local.properties`。也可以手动写入：

```text
QWEATHER_API_KEY=你的和风天气Key
QWEATHER_LOCATION=106.565952,29.642614
QWEATHER_HOST=https://ma5rk8cjh3.re.qweatherapi.com
```

没有 Key 时，天气接口会失败并由前端回退到本地模拟天气。

### 11.3 飞书机器人

部署脚本没有检测到飞书配置时会依次提示：

```text
请输入飞书 APPID（留空跳过）
请输入飞书 APPSECRET（留空跳过）
```

填写后会写入 `config\feishu.local.properties`。也可以复制模板后手动填写：

```powershell
Copy-Item config\feishu.local.properties.example config\feishu.local.properties
notepad config\feishu.local.properties
```

```properties
SMART_QA_API_KEY=你的模型服务Key
SMART_QA_API_URL=https://api.deepseek.com/chat/completions
SMART_QA_MODEL=deepseek-chat

QWEATHER_API_KEY=你的和风天气Key
QWEATHER_LOCATION=106.565952,29.642614
QWEATHER_HOST=https://ma5rk8cjh3.re.qweatherapi.com

FEISHU_ENABLED=true
FEISHU_APP_ID=你的飞书应用ID
FEISHU_APP_SECRET=你的飞书应用Secret
FEISHU_SMART_QA_ENABLED=true
FEISHU_REMEMBER_LAST_CHAT=true
```

飞书功能使用 SDK 长连接模式。部署机需要能够访问公网，不需要配置公网 HTTPS 回调地址。修改配置后重启 Java 服务。

### 11.4 YOLO 人体识别

人体识别不是基础服务启动的必需组件。项目默认需要：

```text
models\yolov8n.pt
workers\human_detection_worker.py
workers\requirements-detection.txt
tools\ffmpeg-9.0.1-essentials_build\bin\ffmpeg.exe
```

人体识别需要 Python。推荐使用 Python 3.11.x；`deploy.ps1 -StartDetectionWorker` 和 `workers\start_human_detection.ps1` 会自动检查 `.venv-detection`，缺少时会：

1. 查找本机 `python.exe`；
2. 未找到 Python 时，自动下载官方 Python `3.11.9` Windows 安装包到 `tools\python-3.11.9-amd64.exe`；
3. 静默安装 Python；
4. 创建 `.venv-detection` 虚拟环境；
5. 安装 `workers\requirements-detection.txt` 中的依赖。

#### 11.4.1 Python 官方下载地址

手动安装 Python 时，只下载 Windows x64 的 Python 3.11 安装包。以下地址是 Python 官方地址：

```text
Python Windows 官方下载页：
https://www.python.org/downloads/windows/

Python 3.11.9 官方发布页：
https://www.python.org/downloads/release/python-3119/

脚本使用的 Windows x64 安装包直链：
https://www.python.org/ftp/python/3.11.9/python-3.11.9-amd64.exe
```

双击安装包时建议勾选 `Add python.exe to PATH`，并保留 `pip` 和 `Python launcher`。如果脚本自动安装，则会自动设置这些选项。安装后重新打开 PowerShell，再检查：

```powershell
python --version
python -m pip --version
```

#### 11.4.2 Python 依赖下载地址

`workers\requirements-detection.txt` 中的依赖由 pip 从 PyPI 下载。项目当前使用：

```text
Python 官方包索引：
https://pypi.org/

Ultralytics：
https://pypi.org/project/ultralytics/

OpenCV Python：
https://pypi.org/project/opencv-python/

Requests：
https://pypi.org/project/requests/
```

手动创建 Python 虚拟环境：

```powershell
python --version
python -m venv .venv-detection
.\.venv-detection\Scripts\python.exe -m pip install --upgrade pip
.\.venv-detection\Scripts\python.exe -m pip install -r workers\requirements-detection.txt
```

如果部署机不能访问 PyPI，可以在联网电脑下载 wheel 文件和依赖，再复制到部署机；或者配置公司内部 PyPI 镜像。不要从不明的第三方网站下载 Python DLL 或打包环境。

#### 11.4.3 FFmpeg 官方下载地址

人体识别 Worker 使用 FFmpeg 生成人体识别回放视频。FFmpeg 官网的 Windows 下载页会列出 Windows 可执行文件构建来源：

```text
FFmpeg 官方下载说明页：
https://ffmpeg.org/download.html

FFmpeg 官网下载页列出的 Windows 构建页（Gyan.dev）：
https://www.gyan.dev/ffmpeg/builds/

当前 release essentials ZIP 直链：
https://www.gyan.dev/ffmpeg/builds/ffmpeg-release-essentials.zip

当前 release essentials ZIP SHA-256：
https://www.gyan.dev/ffmpeg/builds/ffmpeg-release-essentials.zip.sha256
```

项目只需要 `essentials` 构建，不需要下载源码包。可以手动下载并解压：

```powershell
$ffmpegUrl = "https://www.gyan.dev/ffmpeg/builds/ffmpeg-release-essentials.zip"
$ffmpegShaUrl = "https://www.gyan.dev/ffmpeg/builds/ffmpeg-release-essentials.zip.sha256"
$ffmpegZip = Join-Path $env:USERPROFILE "Downloads\ffmpeg-release-essentials.zip"

New-Item -ItemType Directory -Force -Path (Split-Path $ffmpegZip) | Out-Null
Invoke-WebRequest -Uri $ffmpegUrl -OutFile $ffmpegZip
$expectedSha256 = ((Invoke-WebRequest -Uri $ffmpegShaUrl -UseBasicParsing).Content -split "\s+")[0].ToLower()
$actualSha256 = (Get-FileHash -LiteralPath $ffmpegZip -Algorithm SHA256).Hash.ToLower()
if ($actualSha256 -ne $expectedSha256) {
    throw "FFmpeg ZIP SHA-256 校验失败。"
}

Expand-Archive -LiteralPath $ffmpegZip -DestinationPath ".\tools" -Force
```

当前项目默认查找：

```text
tools\ffmpeg-9.0.1-essentials_build\bin\ffmpeg.exe
```

如果解压后的目录名称不是 `ffmpeg-9.0.1-essentials_build`，请把目录改成这个名称，否则使用 `workers\start_human_detection.ps1` 手动指定 `-FfmpegPath`。使用 `deploy.ps1 -StartDetectionWorker` 时，推荐直接保持上述目录结构。

验证：

```powershell
& ".\tools\ffmpeg-9.0.1-essentials_build\bin\ffmpeg.exe" -version
```

#### 11.4.4 YOLO 模型官方下载地址

人体识别需要 `models\yolov8n.pt`。Ultralytics 官方文档和官方模型资产发布页如下：

```text
YOLOv8 官方模型说明：
https://docs.ultralytics.com/models/yolov8/

Ultralytics 官方模型资产发布页：
https://github.com/ultralytics/assets/releases

yolov8n.pt 官方固定版本直链示例：
https://github.com/ultralytics/assets/releases/download/v8.3.0/yolov8n.pt
```

如果项目 ZIP 包中没有 `models\yolov8n.pt`，可以下载后放到：

```text
D:\smoke\models\yolov8n.pt
```

PowerShell 下载示例：

```powershell
$modelUrl = "https://github.com/ultralytics/assets/releases/download/v8.3.0/yolov8n.pt"
$modelPath = Join-Path (Get-Location) "models\yolov8n.pt"
New-Item -ItemType Directory -Force -Path (Split-Path $modelPath) | Out-Null
Invoke-WebRequest -Uri $modelUrl -OutFile $modelPath
Get-Item $modelPath | Select-Object FullName, Length
```

如果固定版本直链失效，打开 Ultralytics 官方模型资产发布页，下载名称为 `yolov8n.pt` 的模型文件，不要下载 `yolov8n-seg.pt`、`yolov8n-pose.pt` 或其他模型替代。

先启动 Java 服务，再启动 Worker：

```powershell
.\deploy.ps1
.\deploy.ps1 -StartDetectionWorker
```

如果只想配置和启动 Java 服务，不想在部署时填写 DeepSeek、和风天气、飞书配置，可以使用：

```powershell
.\deploy.ps1 -SkipOptionalConfigPrompt
```

也可以在一次部署中启动：

```powershell
.\deploy.ps1 -StartDetectionWorker
```

启动前必须存在 `.venv-detection\Scripts\python.exe` 和 `models\yolov8n.pt`。识别截图和视频保存在：

```text
data\detections
```

摄像头必须在“设备管理”中配置为类型 `摄像头`，并填写协议、IP、端口及必要的账号密码。

## 12. 停止、重启和日志

### 12.1 停止服务

```powershell
.\deploy.ps1 -Stop
```

脚本只停止 `tmp\.smoke-server.pid` 指向的服务，并兼容旧版根目录 `.smoke-server.pid`，不会通过端口号误杀其他程序。

### 12.2 查看日志

```powershell
Get-Content .\server.run.out.log -Wait
Get-Content .\server.run.err.log -Wait
```

可选人体识别 Worker 的日志：

```powershell
Get-Content .\worker.run.out.log -Wait
Get-Content .\worker.run.err.log -Wait
```

主要运行文件：

```text
server.run.out.log
server.run.err.log
worker.run.out.log
worker.run.err.log
tmp\.smoke-server.pid
target\
data\detections\
```

### 12.3 重启服务

```powershell
.\deploy.ps1
```

重启不会重新导入数据库。修改 `.env.local`、飞书配置、天气配置或模型配置后，都需要重启。

## 13. 部署脚本参数

查看参数说明：

```powershell
Get-Help .\deploy.ps1 -Detailed
```

常用参数：

| 参数 | 作用 |
|---|---|
| `-InitializeDatabase` | 导入根目录 `mysql.sql`，会覆盖项目表 |
| `-Force` | 跳过数据库覆盖确认，仅应与 `-InitializeDatabase` 一起使用 |
| `-BoardIp` | 开发板 IP；会保存到 `.env.local` 并写入非摄像头设备 |
| `-BoardPort` | 开发板端口；会保存到 `.env.local`，默认 `8888` |
| `-Port` | Java Web 服务端口，默认 `8080` |
| `-MysqlExe` | 指定 `mysql.exe` 完整路径 |
| `-MavenHome` | 指定 Maven 根目录 |
| `-JavaHome` | 指定 JDK 根目录；必须包含 `bin\java.exe` 和 `bin\javac.exe` |
| `-NoPauseOnError` | 出错后不等待回车，适合自动化或批处理环境 |
| `-DatabaseHost` | 覆盖数据库地址 |
| `-DatabasePort` | 覆盖数据库端口 |
| `-DatabaseName` | 覆盖数据库名；初始化时必须是 `farm` |
| `-DatabaseUser` | 覆盖数据库登录账号，默认 `root` |
| `-DatabasePassword` | 覆盖数据库登录密码 |
| `-DatabaseAdminUser` | 覆盖数据库导入账号，默认与 `DB_USER` 相同 |
| `-DatabaseAdminPassword` | 覆盖数据库导入密码 |
| `-StartDetectionWorker` | 启动 YOLO 人体识别 Worker |
| `-OpenFirewall` | 创建 TCP 入站防火墙规则 |
| `-SkipDatabaseCheck` | 跳过数据库网络和 API 检查，仅用于排查问题 |
| `-SkipOptionalConfigPrompt` | 跳过 DeepSeek、和风天气、飞书配置输入提示 |
| `-Stop` | 停止脚本记录的 Java 服务 |

## 14. 常见故障

### 14.1 找不到 `deploy.ps1`

原因是当前 PowerShell 不在项目根目录。执行：

```powershell
Set-Location D:\smoke
Get-ChildItem deploy.ps1
```

### 14.2 找不到 JDK 17

确认：

```powershell
Test-Path .\jdk-17.0.2\bin\java.exe
Test-Path .\jdk-17.0.2\bin\javac.exe
```

如果返回 `False`，说明项目根目录没有放置 JDK。请先按照第 4.1 节从官方地址下载并安装完整的 Windows x64 JDK 17，然后使用安装目录启动：

```powershell
.\deploy.ps1 -JavaHome "C:\Program Files\Eclipse Adoptium\jdk-17.0.20.1-hotspot"
```

也可以将解压后的 JDK 根目录放到项目根目录并命名为 `jdk-17.0.2`。无论采用哪种方式，`-JavaHome` 指向的目录下都必须直接存在 `bin\java.exe`、`bin\javac.exe` 和 `lib\modules`，不能指向 `bin` 目录或压缩包外层目录。

### 14.3 找不到 Maven 或 Maven 下载失败

请从 Apache Maven 官网下载页获取 Maven 3.9.x：

```text
https://maven.apache.org/download.cgi
```

建议下载 Windows 的 `apache-maven-3.9.16-bin.zip`，解压后确认：

```powershell
Test-Path "C:\apache-maven-3.9.16\bin\mvn.cmd"
& "C:\apache-maven-3.9.16\bin\mvn.cmd" -version
```

然后重新运行：

```powershell
.\deploy.ps1
```

如果部署机不能联网，把 Maven 解压到：

```text
项目根目录\tools\apache-maven-3.9.9
```

也可以通过 `-MavenHome` 指定已安装的 Maven：

```powershell
.\deploy.ps1 -MavenHome "C:\apache-maven-3.9.16"
```

### 14.4 找不到 `mysql.exe`

只在初始化数据库时需要。使用：

```powershell
.\deploy.ps1 `
  -InitializeDatabase `
  -Force `
  -MysqlExe "C:\Program Files\MySQL\MySQL Server 8.0\bin\mysql.exe"
```

### 14.5 `Host 'xxx' is not allowed to connect`

这是 MySQL 用户主机授权问题，不是 Java 编译问题。让数据库管理员允许部署机 IP 登录，并检查：

```sql
SHOW GRANTS FOR '用户名'@'部署机IP';
```

授权后重新运行脚本。

### 14.6 数据库 TCP 可达，但 `/api/plots` 失败

依次检查：

1. `.env.local` 中的 `DB_HOST`、`DB_PORT`、`DB_NAME`、`DB_USER`、`DB_PASS`；
2. `DB_NAME` 是否为 `farm`；
3. `farm.plot` 表是否存在；
4. 当前数据库账号是否能访问 `farm` 库；默认使用本机 `root` 账号；
5. `server.run.out.log` 和 `server.run.err.log`。

可以检查表：

```sql
USE farm;
SHOW TABLES;
SELECT COUNT(*) FROM plot;
SELECT COUNT(*) FROM device;
```

### 14.7 端口被占用

查看占用进程：

```powershell
Get-NetTCPConnection -LocalPort 8080 -State Listen |
  Select-Object LocalAddress, LocalPort, OwningProcess
```

如果不是本脚本启动的服务，不要直接停止它；改用其他端口：

```powershell
.\deploy.ps1 -Port 9090
```

### 14.8 首页能打开，但页面显示的是模拟数据

使用真实模式地址：

```text
http://localhost:8080/?simulator=0
```

如果浏览器曾保存过模拟模式，清除该站点的 `localStorage`，或再次访问带 `?simulator=0` 的地址。

### 14.9 开发板离线

先重新运行脚本，确认输入的板子 IP 和端口正确。脚本会把地址写入 `.env.local` 和本机 MySQL 的 `device` 表：

```powershell
.\deploy.ps1
```

检查：

```powershell
Test-NetConnection 开发板IP -Port 开发板端口
```

再检查 `device` 表：

```sql
SELECT id, name, type, ip, port, online
FROM farm.device
WHERE type <> '摄像头';
```

开发板离线不会阻止 Java 页面启动，但不会产生新的实时传感器数据。

### 14.10 人体识别 Worker 启动失败

确认：

```powershell
Test-Path .\.venv-detection\Scripts\python.exe
Test-Path .\models\yolov8n.pt
.\.venv-detection\Scripts\python.exe -m pip list | Select-String "ultralytics|opencv-python|requests"
```

如果 Python 环境缺失，直接让脚本自动准备并启动：

```powershell
.\deploy.ps1 -StartDetectionWorker
```

如果依赖缺失，手动重新安装：

```powershell
.\.venv-detection\Scripts\python.exe -m pip install --upgrade pip
.\.venv-detection\Scripts\python.exe -m pip install -r workers\requirements-detection.txt
```

## 15. 推荐的完整部署命令

以本机数据库 `127.0.0.1:3306`、MySQL `root` 账号、开发板 `10.94.204.29:8888` 为例，推荐流程如下：

```powershell
Set-Location D:\smoke
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass

notepad .env.local

Get-Service -Name MySQL80
Test-NetConnection 127.0.0.1 -Port 3306
Test-NetConnection 10.94.204.29 -Port 8888

.\deploy.ps1 `
  -InitializeDatabase `
  -Force `
  -BoardIp 10.94.204.29 `
  -BoardPort 8888

Start-Process "http://localhost:8080/?simulator=0"
```

以后更新代码或重启服务，只执行：

```powershell
.\deploy.ps1
```

不要重复执行带 `-InitializeDatabase` 的命令，除非确认允许覆盖 `farm` 数据库中的项目表。
